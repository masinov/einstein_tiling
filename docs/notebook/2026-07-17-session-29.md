# Session 29 — W2.D closes the index-45 shell

**Date:** 2026-07-17
**Scope:** exhaust the strong S3 boundary quotients on the next admissible HNF
shell and promote every positive search result through independent DRAT replay.

## Search matrix

The W1 period families exclude 69 of the 78 index-45 HNFs. The remaining nine
were crossed with all 39 inner-conjugacy classes of surjective finalist S3
boundary maps having order-three displacement kernel: 351 coupled relaxed
cover/holonomy scans. The nine maps that killed index 40 were scheduled first,
but the full matrix was allowed to finish.

Every frontier HNF is holonomy-obstructed. Only nine quotient maps kill any
index-45 HNF, forming three exact same-signature triples:

| map triple | kills | misses |
|---|---:|---|
| `{9,15,18}` | 7/9 | `(45,12,1)`, `(45,32,1)` |
| `{21,24,27}` | 7/9 | `(15,4,3)`, `(45,38,1)` |
| `{30,33,36}` | 7/9 | `(15,8,3)`, `(45,6,1)` |

Three quotients—`(9,2,5)`, `(45,19,1)`, `(45,25,1)`—are killed by all nine
maps. Each of the other six is missed by exactly one triple. This is preserved
as finite exact structure, not promoted to an infinite family without a
symbolic derivation.

Search artifact:
`assets/theory-w2-layer-d-s3-index45.json`, SHA-256
`b7dcd50afb6d5551b84e1d6adc124887055f580d34de5b1d0ea27cd0ea59ccfa`.
Review figure:
[`assets/theory-w2-layer-d-index45.svg`](assets/theory-w2-layer-d-index45.svg),
generated by `scripts/render_theory_w2_layer_d_index45.py`.

## Independent certificate gate

For each HNF, the lowest-index killing map was selected. Glucose 4 emitted one
proof for each of its 18 commuting twist pairs: 162 instances. Every raw proof
verified under `drat-trim`; every extracted core CNF/core proof reverified;
the generic standalone verifier regenerated the canonical CNF, checked the
core as a multiplicity-respecting clause subset, checked compressed and
uncompressed hashes, and replayed all 162 stored proofs.

The certificate directory contains 324 files and occupies approximately 361
MiB on disk; the manifest reports 377,474,096 compressed bytes. Manifest:
`assets/theory-w2-layer-d-proof-index45.json`, SHA-256
`196a9e20efcc937e03ab47d7fa7ff3e6b3e40ec18ce4c723a88ff7deafc08f04`.

## Result and scope

At index 45 the exact split is 69 W1-family exclusions plus nine independently
certified Layer-D exclusions. Area eliminates indices 41--44. Combined with
the certified prefix through 40, this proves that no grid-aligned finalist
exact cover exists on any HNF torus of index at most 45.

This is not O1 and not a proof of aperiodicity: larger fundamental domains
remain possible. The next mathematical task is to derive the three map
signatures from S3 generator images, deck exponents and HNF congruences before
spending on the 18-HNF index-50 frontier.

Decision: D-0037. Producers:
`scripts/run_theory_w2_layer_d_index45.py` and
`scripts/run_theory_w2_layer_d_proofs_index45.py`; replay:
`scripts/verify_theory_w2_layer_d_proofs.py --manifest
docs/notebook/assets/theory-w2-layer-d-proof-index45.json`.

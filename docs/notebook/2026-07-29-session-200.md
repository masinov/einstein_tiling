# Session 200 — finite-automaton contact abstraction

## Authorization and scope

HC-2026-07-29-51 authorizes sessions 200--202 only to close the current
AHI/zipper branch: extract a source-independent theorem, audit its prior-art
boundary, consolidate the logical residue, and freeze the branch. No polygon
family, reset-count case, source specialization, experiment, or search is
authorized.

## Done

- Proved K74A: a rooted three-participant T-junction chain compiles any finite
  automaton at fixed word length.
- Proved K74R: deterministic prefix tries give every finite fixed-arity
  relation with a unique lifted role word.
- Proved K74G: finite group products are a special case, identifying K70Z as
  the `Z/2`, length-three instance rather than a special AHI mechanism.
- Recorded the architecture-independent local-expressivity hierarchy:
  independent two-body profiles give bicliques; participant-separable
  torsion-free sums miss parity; hidden finite state at rooted T-junctions
  gives all finite relations.
- Froze the AHI-specific carrier ladder in D-0227.

## Verification

The construction is symbolic and exact. For consecutive transitions ending
in state `r` and beginning in state `s`, the three sector angles sum to

```text
pi + (pi-lambda(r)) + lambda(s)
  = 2pi + lambda(s)-lambda(r),
```

which is `2pi` exactly if and only if `r=s`, because all state angles are
distinct. Delimiters impose initial and final states. Both directions of the
path/cover bijection are written in theory note 82.

No research computation was run; D-0065 does not apply.

## Claim boundary

This is a coloured/role-labelled rooted contact complex. It assumes the host,
cover order, unit subdivision, vertex alignment, and exactly three
participants. It does not produce one unmarked polygon, classify unintended
contacts, prove a total decoder, prove tileability, or prove aperiodicity.
The construction is standard finite-state semantics; no novelty is claimed.

## Open for the branch closeout

- Complete the dated primary-source audit.
- State the permitted/forbidden public claims.
- Consolidate the AHI branch by theorem obligation and freeze it formally.

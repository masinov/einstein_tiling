# Session 199 — select the outward fan and prove the port-curvature no-go

## Done

- Derived K73F, the `C=7pi/6` outward-bending specialization of the exact
  phase zipper.
- Gave its complete endpoint table and exact `Q(sqrt(3))` host coordinates.
- Distinguished its divergent exterior-normal order from K72S's convergent
  reflex branch.
- Proved N73W: no polygon can carry all six required directed roles as
  unbroken convex-flank ports, even with the sole compatible adjacency.
- Proved K73R: the retained nonadjacent carrier has residual signed turn
  `-19pi/6` and at least four reflex vertices; the adjacency relaxation still
  needs three.

## Verified

- Matched endpoint sums are `7pi/6`; mismatches are `5pi/4` and `13pi/12`.
  Adding host angle `5pi/6` accepts only the matched case.
- The host directions `0,pi/6,pi/3,pi/2,2pi/3` give the five outward normals
  `-pi/2,-pi/3,-pi/6,0,pi/6` in divergent cyclic order.
- The six pairwise-nonadjacent port turn contributions are
  `(10+11+9+10+12+10)pi/12=31pi/6`.
- Subtracting the simple-polygon total `2pi` leaves `-19pi/6`; three reflex
  vertices cannot supply that much negative turn.
- Endpoint compatibility allows only a right `pi/2` endpoint followed by the
  left `pi/2` endpoint of the unique `D_L`; identifying it once leaves
  `14pi/3` positive turn and residual at most `-8pi/3`.

## Failure / boundary

- The natural all-convex port-wedge realization is impossible.
- N73W does not close polygons whose port flanks undergo reflex resets.
- No common polygon, patch atlas, source lift, tiling, or monotile is claimed.

## Open

Give one complete exact reflex-reset comb with at least four reflex vertices
and all four disjoint placement tables, or prove a no-go for a precisely
specified reset topology.

No experiment, search, solver, rendering, or candidate promotion was used.

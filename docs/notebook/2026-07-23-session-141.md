# Session 141 — HC-30 unequal-guard transfer assessment

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-30`, session 3 of 3

## Proposition

After the primary K16W decomposition succeeded, assess the authorized
secondary hypothesis: replace K22S's equal guard-lens sides by independent
lengths `e,f`, retain internal spoke length `d`, and determine exactly which
parts of K25X/N37 transfer before any search.

## Results

1. **K28G.** The guard and its half-turn bound the exact parallelogram with
   basis vectors `e` and `f*exp(i*(pi-gamma))`. K22R's six partial sums are
   unchanged; containment, host closure and 66 simplicity predicates give a
   finite exact conditional family. It recovers K22S at `e=f=d`.
2. **K28T.** K25X's intersection parameters remain
   `b/(2*d*sin(gamma/2))` and its complement. But a displacement with unit-
   basis coefficients `(r,s)` now changes normalized guard coordinates by
   `(d*r/e,d*s/f)`. Containment and crossing no longer share one scale, so
   N37 does not transfer.
3. An exact non-right control with half-angle `(4/5,3/5)`,
   `(d,b,e,f)=(1,2,10,11)` simultaneously admits every isolated spoke/code
   direction by strict oblique-coordinate bounds and avoids the K25X
   crossing. It is not a coupled spine witness.
4. The new `e,f` sides alter the polygon's intrinsic role inventory and
   cover tables. K28W freezes the family before geometry: complete contact-
   role compatibility and local recognizability must be proved first.

## Verification

- `sin(gamma)=24/25`, so every unit direction has each oblique coefficient
  at most `25/24` in absolute value.
- The largest local displacement in the control is `2*(25/24)=25/12`,
  strictly below `e=10` and `f=11`.
- `2>6/5` proves the named spoke pair is outside the closed K25X
  intersection interval.
- No coupled containment, host closure, simplicity, patch, decoder or
  aperiodicity claim is inferred from this local separation control.
- Governed artifact roots remain byte-identical to the HC-30 baseline:
  `docs/notebook/assets=6756513353`, `data=1809060890`.
- Repository tests: `224 passed, 17 deselected`; the focused governance suite
  reports `4 passed`.

## Decision

HC-30 is exhausted at 3 of 3 sessions. The primary K16W branch now has an
exact twelve-cell critical-pair reduction but remains open/frozen. The
secondary unequal-guard family survives N37 only conditionally and freezes
at K28W's contact-role theorem. No experiment, artifact, polygon, candidate
or monotile result was produced.

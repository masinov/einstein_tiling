# Session 106 — HC-17 unequal stems require a cap

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-17`, session 1 of at most 3

**Mode:** audit correction and on-paper local topology; no experiment

## Proposition

Correct the K7C strip overstatement, then decide whether unequal first stem
lengths can remain a two-neighbor collar or necessarily create another
participant.

## Prior art and non-redundancy

The user supplied an exact internal contradiction, so ERR-007 is a halt gate
and is resolved before new work. The new result is only the local topology at
the first mismatched stem endpoint; it makes no novelty claim for
T-junctions or edge-patch methods.

## Result

ERR-007 restricts K7C's rectangular conclusion to
`(0,L) x (-d,0)`. The reflex spillover of `A` belongs to `Q_A`; if no third
occurrence participates at that host endpoint, its `3*pi/2` angle forces the
host corner to `pi/2`.

N22 proves that an unequal stem necessarily leaves a nonzero sector outside
the two original neighbors at the shorter-side endpoint. Gapless coverage
therefore supplies a secondary participant. If exactly one cap fills it, its
angle and the shorter tile's angle sum to `pi`; in the orthogonal case both
are right angles.

No cap geometry or polygon is claimed.

## Decision and stop rule

Sessions 107--108 may solve the fixed finite question: can all four primary
adjacencies of `ABC,ACB` use one recognizable stem excess and one cap
orientation that closes without a further junction? Failure freezes this
route. No contextual handedness, unbounded cascade, coordinate search, SVG
or candidate promotion is authorized.

## Housekeeping and verification

- Recorded HC-17 at the session-105 byte-identical artifact baseline.
- Removed the reviewed zero-byte Conway--Lagarias fetch remnant; the intact
  PDF remains.
- No research runner or generated research artifact was created.

# Session 82 — exact boundary-cocycle factorization of K1P

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-09`, session 1 of at most 3

**Mode:** on-paper contextual-junction design only; no polygon, contact
census, search, drawing, experiment or generated artifact

## Admission and kill condition

HC-09 allows three sessions to identify a concrete contextual or ternary
mechanism surviving N7--N9. Failure closes the exact-compiler route. The
audit's six-valent-vertex suggestion is treated as a prompt, not a theorem.

## K2C

K1P's codebook is the disjoint union of the 32 base even-parity words and
fresh diagonal words for every remaining state. Put a `Z/2` potential at each
carrier corner. Corner constraints force all three modes onto the base branch
or onto one common fresh tag. On the base branch, a side mode's parity is the
difference of its endpoint potentials.

Summing the differences around the closed tile gives even parity. Conversely,
every even word integrates to corner potentials, uniquely up to simultaneous
flip. The fresh branch closes by tag equality around the same cycle. This
proves K2C: the cyclic factorization projects to exactly the complete K1P
tile-star relation.

## Decision

K2C survives N7--N9: it is contextual, uses only gauge-invariant differences,
and supplies a variable physically shared by adjacent sides. The next session
must test shared six-valent vertices. If their sector states are inadvertently
identified, the lift may collapse and the candidate fails.

No `EXPERIMENTS.md` entry is added.

## Verification

- telescoping proves necessity of even parity;
- direct integration proves sufficiency and the two-element gauge fiber;
- three-cycle tag equality gives exactly the fresh diagonal words;
- the union is exactly the K1P codebook defined in note 11;
- `git diff --check` and governance tests passed;
- no research command or generated artifact was created.

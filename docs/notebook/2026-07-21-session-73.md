# Session 73 — lossless contact-incidence baseline for K1

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-06`, session 1 of at most 3

**Mode:** on-paper symbolic construction only; no experiment, collar
enumeration, carrier geometry, or generated artifact

## Question

Can the full addressed S0 source be moved from tile-interior colors to a
finite coupled contact-star presentation with a total inverse on every legal
configuration, before attempting any unsafe state merge?

## Result: ST-M1.K1C

For every legal source adjacency, record the two addressed endpoint states
and directed sides as an involutive half-contact. Require the three contacts
at one triangle to share one source state, and retain the transported legal
cyclic corner words. Encoding and decoding are inverse radius-one maps.

This gives a lossless contact-incidence presentation of S0. The shared center
state couples all three directions, so the independent-rail no-go does not
apply. The inverse is defined on the complete stated contact-rule space, not
only on intended source images.

## Claim boundary

K1C is a general recoding, not a nontrivial quotient and not a novelty claim.
Its contact records remain symbolic colors. It proves neither that one fixed
unmarked polygon realizes those contacts nor that any two records can be
merged safely.

The next on-paper unit is the future-equivalence/local-inverse theorem for a
proposed quotient. No radius or collar table is authorized.

No `EXPERIMENTS.md` entry is added. Artifact roots remain at the HC-06
baseline.

## Verification

- `docs/HUMAN_CHECKPOINTS.json` records HC-06 through session 72 and parses;
- artifact roots remain exactly `6756125300` and `1793649957` bytes;
- `git diff --check` passed;
- governance tests: `4 passed`;
- full fast suite: `222 passed, 17 deselected in 145.16s`;
- no research command was run and no generated artifact was added.

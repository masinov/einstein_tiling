# Session 39 — W3 certificate kernel and exact Spectre geometry

**Date:** 2026-07-19  
**Scope:** return to W3 after the Hall-method falsification; formalize only the
parts of the Spectre control that already admit cold exact verification.

## Finite substitution audit

The A6 17-state radius-one collared substitution is deterministic, closed and
primitive.  Exact Boolean matrix powering gives least primitivity exponent 3.
The new versioned certificate deliberately remains `VALID_PARTIAL_CERTIFICATE`:
sampled forcing is not promoted to an exhaustive legal language.

## Geometry recurrence

The vendored construction on four module points is integer-linear in sixteen
coordinates.  Reconstructing its matrix from basis quads yields an exact
unimodular recurrence.  Its minimal polynomial is

```text
x^8 - 10 x^6 + 18 x^4 - 10 x^2 + 1
 = (x^2 - 1)^2 (x^4 - 8 x^2 + 1).
```

The recurrence reproduces all eight child translations at all 32 materialized
levels: 256/256 exact vector comparisons pass.  This separates a stationary
combinatorial substitution from its level-dependent, non-similarity geometric
realization.

## First exact C1 layer

An exact `Z[sqrt(3)]` polygon checker now expands physical leaves and verifies
interior disjointness, edge-manifold incidence and contact connectivity.  All
nine labels at levels zero and one pass.  The result is recorded as a finite
prefix only; an all-level macro-boundary induction remains the C1 blocker.

The named rules reduce inductively to two geometric types, full and missing.
Their abstract edge complexes are disks through level four. A single
distinguished boundary side satisfies the same five-piece dihedral word
recursion through levels 2--5, with lengths obeying
`a_n=4a_(n-1)+a_(n-2)`. This is now the precise candidate induction; it is not
yet promoted beyond finite evidence.

Child-owner tracing further produces a four-side macro grammar stable from
level one. All of its endpoint-chain identities have now been promoted from
finite evidence to an all-level algebraic result: the identities are linear in
the quad, and eight initial checks plus the matrix annihilator prove them on
the whole recurrence orbit. The remaining C1 issue is genuinely geometric:
endpoint gluing does not alone prove simplicity or exclude transverse
intersections of recurrent boundary arcs.

Targeted tests: 12 passed.  The standalone certificate verifier passes while
retaining all four non-C2 blockers.

## Interrupted diagnostic

The old `w2_d7_taper_r2` screen is now a dead socket and no Python process or
radius-two output artifact survives.  Because a stronger full-nonoverlap Hall
countermodel already refuted the branch's target conclusion, the interrupted
taper is not restarted; doing so would consume time without changing any open
theorem obligation.

## Next

Derive the complete direction-word recursion from the exact macro grammar and
prove the resulting cycles have no transverse crossings. If it
closes, derive a certified inball recurrence from the same
boundary data; otherwise record the precise boundary-state enlargement needed.

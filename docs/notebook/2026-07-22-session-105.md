# Session 105 — HC-16 tail-packing stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-16`, session 3 of 3

**Mode:** on-paper exact geometry; no experiment

## Proposition

Close the two K7C rooted-tail packings with one exact irredundant orthogonal
polygon and hand-verifiable host-plus-three placements for `ABC` and `ACB`.

## Prior art and non-redundancy

The edge-patch and symbolic compiler frameworks remain prior art. The only
admitted result would be the fixed one-support coordinate witness stated in
session 103. A valid angle table or collar alone is not that witness.

## Result

No exact coordinate set closes all six tail intersections in both K7C offset
triples while also avoiding the host occurrence. The local K7A selector and
K7C collar remain correct, but the first unresolved geometry begins below the
collar. No sketch or partial coordinate family is promoted.

This failure is not a proof that arbitrary nonconvex selected-word carriers
do not exist.

## Decision

The HC-16 stop fires exactly as predeclared. K7A and K7C are retained as an
exact local binary channel and geometric reduction. K7W, the actual polygonal
witness, is recorded open/frozen. No search, optimizer, SVG, generated
artifact, candidate tile or monotile claim follows.

Reopening the same route requires exact coordinates and both complete
placement lists before computation. Unequal stems, contextual handedness or
extra participants change the mechanism and require a separately authorized
checkpoint.

## Verification

- The final status distinguishes a missing witness from a nonexistence
  theorem.
- Full fast suite: `222 passed, 17 deselected` in `146.39 s`.
- `git diff --check`: clean.
- Checkpoint roots are byte-identical to the HC-16 baseline:
  `docs/notebook/assets = 6,756,125,300` bytes and
  `data = 1,809,060,890` bytes. No research artifact was generated.

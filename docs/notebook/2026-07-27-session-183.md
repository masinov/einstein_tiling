# Session 183 — test the 17-rhombus polygon as a rep-3 source tile

## Experiment pre-registration

### Proposition

Test two exact statements jointly:

1. the 51-rhombus common support of Figure 45 is an image of the K56C
   17-rhombus support under a triangular-lattice similarity of determinant
   three (scale `sqrt(3)`), up to full lattice isometry and translation; and
2. each of the two displayed `3 large_A + 6 singleton_M` decompositions can
   be partitioned into three interior-disjoint K56C blocks, each consisting of
   one displayed `large_A` plus two displayed singleton `M` tiles.

### Prior art and non-redundancy

The paper records the 51-rhombus local flip but does not identify the newly
derived 17-rhombus common compiler or a one-support rep-tile substitution.
K56C supplies the exact small support and K55I supplies the exact large
support.  The area ratio three makes the similarity/partition question finite
and source-native; no polygon search is involved.

### Outcome decisions

- If both statements hold, promote the 17-rhombus support only as a concrete
  connected unmarked **candidate with two exact rep-3 dissections and proved
  tilability by substitution**.  Aperiodicity still requires unrestricted
  periodicity rejection and a total recognizable decoder; neither follows
  from existence of a substitution.
- Otherwise preserve the legal K56C source relation but do not claim a
  self-similar tile.

### Stop rule and finite justification

Enumerate the twelve unit/reflection associates of one fixed determinant-3
Eisenstein similarity and translations aligning exact vertices.  For each of
the two fixed 51-rhombus assemblies, enumerate only assignments of its six
serialized singleton tiles into three unordered pairs attached to its three
serialized `large_A` tiles.  Require exact support equality and disjoint
coverage.  Do not search any other scale, support, or dissection.

### Human checkpoint

`HC-2026-07-27-45`, session distance three.  This directly decides whether
the first legal source compiler kernel is an actual self-similar tile.

## Results

The determinant-three similarity test has zero witnesses in both 51-rhombus
panels, so the large envelope is not a `sqrt(3)` inflation of the fixed
17-rhombus support.  Nevertheless, each panel has an exact partition into
three copies of that support, and each copy consists of one displayed
`large_A` plus two displayed singleton `M` tiles.

## Verification

The cold verifier rebuilds both support comparisons and both exact-cover
partitions.  Artifact:

```text
data/sturmian-source/ahi-seventeen-rhombus-rep3.json
sha256 4032ff41b2d12909abcaa0324d2a1f716899c050fde2f4032dc296ed15a4de15
```

## Decision

Retain a genuine three-tile local flip, but no self-similar substitution or
whole-plane tiling claim.

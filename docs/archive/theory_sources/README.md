# Chronological theory proof sources

This directory preserves the original numbered derivations `07`--`82` and
the former monograph outline.  They are immutable proof provenance after the
2026-07-29 theory consolidation.

The numbered sequence reflects research chronology and contains corrections,
superseded branches and architecture-specific notation.  Begin instead at
[`docs/theory/README.md`](../../theory/README.md).  The canonical theory states
the reusable results and scoped applications; the original notes remain here
so no proof detail, exact coordinate argument, counterexample, or historical
qualification is lost.

`docs/theory/reference/SOURCE_MAP.json` assigns every source to a canonical
domain.  `docs/theory/reference/proof_ledger.md` retains its theorem IDs and
evidence paths.

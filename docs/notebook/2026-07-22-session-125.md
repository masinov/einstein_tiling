# Session 125 — square-lens obstruction and early HC-23 stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-23`, session 2 of at most 3; closed early

**Mode:** on-paper exact geometry; no experiment

## Proposition

Test the fixed K13F/K10B first `A,d` turn and central `H` edge against the
square lens before attempting a coordinate list.

## Result

N31 proves two necessary inequalities:

```text
d <= sqrt(2)*a,             h <= sqrt(2)*d.
```

The first follows because the initial `A` edge starts in the square tangent
quadrant and the next `d` edge turns by `+/-3*pi/4`; one endpoint coordinate
is at most `a-d/sqrt(2)`. The second is the square-diameter bound. Hence a
K10B host with `h=a+b+c` requires `b+c<=a`.

The fixed tuple `(1,4,6,11;d=12)` violates the prefix and combined bounds.
Moreover every K13F member `(1,n,n+2,2n+3)`, `n>=4`, violates the combined
bound, so none can use the unchanged K10B square-lens topology for any `d`.

## Decision

HC-23's stop rule fires in session 125. No session 126 geometry attempt is
needed. The arithmetic family K13F survives; the K13F/K10B geometric pairing
is closed. Reopening must change a named N31 hypothesis under a new human
checkpoint.

## Verification

- Equations (6.3)--(6.4) cover both reflected signs of the first turn.
- The second bound is the exact diameter of a side-`d` square.
- No coordinate fitting, experiment, SVG or artifact was generated.
- Artifact roots remain byte-identical to the HC-23 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.
- Repository tests: `222 passed, 17 deselected` in 145.33 seconds.

# Session 193 — directional corridor charge and its exact ceiling

## Done

- Proved K67O: the three lozenge-orientation counts of a finite triangular
  support are fixed by its boundary/primitive-cell set, independently of the
  perfect matching.
- Used the exact source quotient to prove K67D:
  `S_j=2k`, `L_j=k`, and `M_j=N_j-3k` on every axis.
- Proved K67G, the exact graph decomposition
  `N_j-3k=chi_j+2p_j`, separating exposed Gauss charge from hidden transition
  pairs.
- Proved N67C: corridor endpoint data determine transition count only modulo
  two, so parity cannot close even count-changing trades.

## Verified

- Both cold-verified large source macros have exactly two `S` and one `L`
  rhombus on every diagonal axis. Their remaining axiswise `M` distributions
  are respectively a permutation of `(0,3,3)` and `(2,2,2)`.
- The barycenter-displacement proof fixes `(N_0,N_1,N_2)` without assuming
  simple connectivity or flip connectivity of the lozenge-tiling space.
- On each continued SAB axis graph, transition parity is exactly the sum of
  the exposed odd-degree endpoint bits; all interior degrees are even.
- The fixed-endpoint words `000` and `010` prove that no endpoint-only charge
  can see the hidden transition-pair integer.

## Failures / boundaries

- The hoped-for all-area conservation theorem does **not** exist at the
  independent corridor-path level. It detects odd trades only.
- N67C is not a witness for an even AHI trade: the complete coupled vertex and
  macro exact-cover rules may forbid the generic binary excursion.
- A charge requiring identical marked carrier boundaries would not close a
  contextual unmarked compiler; the distinction remains explicit.

## Open

Decide the single coupled defect: can the complete AHI three-rail vertex/SAB
language create three synchronized transition pairs per two-unit change in
large-macro count? A no-go closes the boundary-neutral carrier-local branch;
an exact patch is the first constructive even count-changing trade.

No experiment, solver, carrier-area census, polygon, or candidate was opened.

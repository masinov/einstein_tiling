# Session 79 — pure pose cannot realize the K2H parity orbit

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-08`, session 1 of at most 3

**Mode:** on-paper pose-action analysis only; no polygon, contact census,
search, drawing, experiment or generated artifact

## Audit prompt and correction

The audit suggested that pure rotations cannot carry `H=(Z/2)^2` because
`H` does not embed in a cyclic rotation group. A group embedding is not
required merely to label four selected poses, so that argument is not adopted.

The correct equivariant obstruction concerns the fixed boundary. In a pure
pose encoding, one reference side-parity triple is transformed only by
permuting its three interface directions. Every such permutation preserves
Hamming weight, whereas K2H needs `000` and the three weight-two patterns.
No single orbit supplies both layers. This proves N7 even if reflections are
temporarily allowed.

## G6 quantifier

The K1P core is fixed by choosing one source witness `y_0` and one large macro
type `tau` occurring in it. G6 must name a lift containing that same `tau`.
The code is then fixed on all essential states; no claim that both large
types occur in all tilings is needed.

## Decision

Pure intrinsic pose is closed. HC-08 next examines the natural translation
phase `L/2L` and states exactly when its origin ambiguity prevents local
decoding. Context-dependent docking states remain open.

No `EXPERIMENTS.md` entry is added.

## Verification

- the complete permutation group `S_3` preserves Hamming weight;
- the four even Boolean patterns occupy weights zero and two;
- the result is stronger than needed with respect to reflections but does not
  claim a no-go for contextual pose/contact stars;
- `git diff --check` and governance tests passed;
- no research command or generated artifact was created.

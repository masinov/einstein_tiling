# Session 191 — complete area-30 carrier-local classification

## Experiment pre-registration

### Proposition

Exhaust every area-30 carrier support whose mandatory K65A `H` state is a
connected topological-disk union of two exact published large macros.  The
first macro ranges over `large_A,large_B`; the second ranges independently
over both types and every full lattice isometry, subject only to disjoint
primitive-triangle interiors and a shared unit boundary edge.

For every resulting support, enumerate all alternative carrier-local states:

1. every all-singleton `Z=(0,30)` perfect matching; and
2. every `G=(1,15)` state obtained by embedding either exact large macro in
   the support and perfectly matching the residual 30 triangles.

For each alternative, test the necessary bipartiteness of its singleton
long-diagonal continuation graph.  No AHI endpoint-germ or vertex filtering
is applied before this test, so the input is a geometric superset.  Zero
parity survivors proves no carrier-local area-30 compiler exists.  A survivor
is only a finite exact input to the full source-rule check, not a tile.

### Prior art and non-redundancy

K64A--N64S completely close the same realization family below area 30.  K65A
proves that area 30 necessarily contains a two-large state and at least one
`G` or `Z` alternative.  The primary AHI source gives the exact two large
supports and singleton continuation rule.  Existing certificates cover one
17-rhombus equalizer and the complete sub-30 attachment class; none classifies
all two-large unions or their area-30 alternative dissections.  The run is the
next exact composition case and has no adjustable radius, candidate ranking,
or polygon search.

### Outcome decisions

- If no `G` or `Z` alternative has a bipartite singleton graph, preserve an
  odd-cycle witness for every alternative and promote the exact area-30
  carrier-local impossibility theorem.
- If alternatives survive, preserve every support, embedded large macro, and
  singleton diagonal set.  Those survivors become the complete input to the
  exact AHI endpoint-germ/vertex check; no shape promotion is authorized.
- If support generation or perfect-matching enumeration exposes an
  ambiguity, fail closed and record it.  Do not restrict contacts or macro
  orientations to obtain a result.

### Stop rule and finite justification

Enumerate exactly the four ordered macro-type pairs, all twelve point-group
isometries, and translations obtained by boundary-vertex alignment.  Retain
only interior-disjoint connected disks.  For each fixed 60-triangle support,
enumerate all contained large-macro embeddings and all perfect matchings of
the corresponding 30- or 60-triangle residuals.  Stop after this area-30
class and one cold rebuild.  No larger area, contact radius, solver, timeout,
or source-rule relaxation is authorized by either outcome.

### Human checkpoint

Checkpoint `HC-2026-07-28-48` records through session 190 with the current
area-30 normal form as the only authorized carrier-local continuation.  This
is session distance one and the expected finite artifact is below the 1 GiB
budget.  The user's persistent instruction is to follow this exact route to a
Sturmian monotile or a clearly scoped impossibility/undecidability theorem.

## Results

The gate passed before implementation.  The producer was launched once
through `scripts/run_research.py` and returned:

```text
supports=65
G embeddings=164
G perfect matchings=3390
Z perfect matchings=48652
parity survivors=0
```

The 65 supports are full-isometry classes.  Their provenance covers all four
ordered macro-type pairs: 10 `A/A`, 25 `A/B`, 25 `B/A`, and 30 `B/B`
incidences (one support may have multiple incidences).  No endpoint-germ or
vertex filter was applied.  Disconnected or holed singleton residuals of a
contained large macro were retained and matched exactly.

## Verification

The cold verifier rebuilt the complete support and matching classification:

```text
verified area-30 carrier classification:
supports=65 G_embeddings=164 G_matchings=3390
Z_matchings=48652 survivors=0
```

Artifact pin:

```text
ahi-area30-carrier-classification.json
sha256 adc403154e98a70205ed58284e54de36d795466635489e91cbb03e2a7ad1bab1
bytes  905959
```

Targeted governance/carrier tests pass (`11 passed`).  The complete repository
suite passes:

```text
283 passed, 17 deselected
```

## Decision

Promote K65C/N65S: area 30 is impossible in the carrier-local AHI realization
family.  K65A proves that every viable library requires a two-large state and
one of the two alternative classes exhausted here; both have zero necessary
parity survivors.

Do not continue by enumerating area 31 mechanically.  The result instead
motivates the all-area structural question: every viable fixed-area library
must straddle the irrational composition slope, hence must exchange at least
one large macro against 15 singleton cells across two carrier states.  The
next theorem target is whether the AHI corridor cocycle supplies a conserved
boundary charge or other general obstruction to every such finite exchange.

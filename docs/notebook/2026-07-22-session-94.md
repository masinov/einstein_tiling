# Session 94 — HC-13 provenance repair and synchronizing retiling kernel

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-13`, session 1 of at most 3

**Mode:** primary-source repair and on-paper inverse dissection; no experiment

## Audit precondition

The Hu--Lin PDF was recached through the repository fetcher from the archived
AMS primary copy. Its SHA-256 is
`3f46f0e8f483f87a852f90bc28c0a51a0c798682ff1b4de4e50b1af09b7d5bbd`,
matching the independent audit exactly. The extracted text reproduces
Theorem 2.3's 17/56 corner classifications. The zero-byte failed temporary
file was removed.

The Kari--Moutot source defines `S={2^j-1}` for `0<=j<=t-1`, so its largest
index is `s=2^(t-1)` and `N=3s`. The review and session 92 now use that
formula. N12 is unchanged because it used only existence at a sufficiently
large finite width.

## Mathematical result

Theory note 19 gives one exact inverse-dissection kernel. A `2 x 16`
rectangle has 12 selected rooted domino retilings

```
VVV H w H VVV,
```

where `w` is a width-six composition by `V` (width one) and `H` (width two),
excluding the all-`V` word. The elementary recurrence gives 13 composition
words and hence 12 admitted states, all with exactly 16 dominoes.

On concatenation, adjacent three-column collars make a unique maximal
six-column vertical-domino marker. Under four explicit guarded-language
conditions, its midpoint gives a unique finite-radius macro grouping. This
is a physical-occurrence synchronizer rather than an origin-dependent
lattice phase.

## Boundary

The bare domino does not force the guarded language, and this one-dimensional
word carrier does not yet expose four Wang edge colors. No tile candidate,
boundary word, contact atlas, lift or aperiodicity claim is made. Sessions
95--96 must either supply a credible geometric/topological forcing route for
the marker and four interfaces, or fire HC-13's kill for this
synchronized-domino parameterization without a shape run.

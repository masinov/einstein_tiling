# Session 120 — exact ABBB collar and HC-21 stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-21`, session 3 of 3

**Mode:** on-paper fixed-radius closure; no experiment

## Proposition

Determine whether any existing radius-one geometry excludes N29's fourth
word, without coordinates or context escalation.

## Result

N30 constructs an exact interior-disjoint collar along `H` for `ABBB`.
At the three transitions `AB,BB,BB`, the host fills one half-plane and the
code/guard sectors fill the other because
`rho_X+gamma+ell_B=pi`. The separated junction neighborhoods join through
thin side collars. Its independent left `A` and right `B` endpoint roles are
already accepted by the intended language.

This proves that sector and endpoint arguments cannot reject the word. It
does not place complete congruent polygons: those could collide away from
`H`, but no coordinate-free collision theorem or exact K10B polygon exists.

## Decision

HC-21's fixed-radius stop fires. K12C remains open/frozen and the factorized
radius-one route is closed. Radius two is not attempted. Reopening requires
an exact full-occurrence collision theorem or a complete polygon and full
radius-one placement tables supplied before work.

No coordinates, alternative boundary, experiment, SVG or candidate was
produced.

## Verification

- Each internal sector sum is exact by K9A equation (3.1).
- Positive separation of the points `1,3,5` gives disjoint small junction
  neighborhoods and a common thin collar.
- Full fast suite: `222 passed, 17 deselected`.
- Artifact roots remain `docs/notebook/assets = 6,756,125,300` bytes and
  `data = 1,809,060,890` bytes.

# Session 81 — close the unanchored frame-pose shortcut

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-08`, session 3 of at most 3

**Mode:** on-paper synthesis only; no polygon, contact census, search,
drawing, experiment or generated artifact

## N9

Suppose the K2H phase depends only on a carrier's finite orientation and its
absolute `L/2L` residue in the bare recovered frame. Translation equivariance
forces invariance under all global residue shifts, so the state cannot depend
on that residue. It then reduces to pure orientation, which N7 excludes by
Hamming-weight preservation. The combined pose shortcut therefore fails.

## Remaining geometric obligation

An auxiliary state reduces geometric arity only when one independently
visible central feature is shared by all three interfaces. If `h` is merely
reconstructed after reading the three side modes, existentially eliminating
it gives the original parity relation and N5 remains unchanged.

The exact-compiler route now has two honest possibilities: construct a bounded
four-class contextual star feature, or construct a genuine ternary/larger-
radius exact-cover junction. Neither is presently identified.

## Decision and stop

The pose-action question is closed negatively. K2G is frozen until an
on-paper candidate for one of those two mechanisms exists. No collar
escalation, notch drawing or contact search is justified by the current
symbolic factorization.

HC-08 is exhausted. No `EXPERIMENTS.md` entry is added.

## Verification

- global translations act transitively on `L/2L` while preserving orientation;
- residue invariance reduces the proposed state to the N7 orientation case;
- the remaining `eta` obligation is stated as an actual local geometric map,
  preventing an existential proof variable from being mistaken for a shape
  mechanism;
- `git diff --check` passed;
- governance and full fast tests passed;
- artifact roots remained byte-identical to the checkpoint baseline;
- no research command or generated artifact was created.

# Session 157 — HC-36 transverse root and strand law

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-36`, session 2 of 3

## Admission

This remains theorem-only work under D-0178.  The exact question is whether
the complete K32S strand signs distinguish K35D's two transverse roots.  No
solver, CAS, numerical sweep, coordinate fitting or experiment is admitted.

The accepted outcome is an exact root-selection implication or a precise
criterion showing which additional sign must be proved.  A suggestive plot,
sampled angle or claim that the two roots are “usually” separated is invalid.

## Result

K36W writes the rectangle diagonal in either relative terminal frame as

```text
(T*w_8-epsilon*sqrt(Delta)*i*w_8)/|w_8|^2.
```

K36M uses that identity to express the B and C midline offsets exactly as
affine radical signs.  The known east/west polarities fix their denominators,
so S1--S3 translate without an orientation ambiguity.

K36P multiplies the two conjugate signs.  A negative value of
`F_j^2-Delta*Q_j^2` forces the two roots onto opposite sides of H for strand
`j`; a fixed strand cell then accepts at most one root.  A positive value
leaves both roots on the same side.  Equality is a forbidden B/C contact with
the center of H.

## Verified

The derivation uses only rotation invariance of dot products and determinants,
the exact K35D root formula, and the already-proved signs
`Re(Z*rho_B)>0`, `Re(Z*rho_C)<0`.  Multiplying the two radical conjugates gives
the radical-free expression directly.  No research command was run.

## Result boundary

No existing theorem fixes either conjugate product's sign throughout a live
cell.  Therefore no transverse root or K34Q cell is removed, and the
conditional `2 -> <=1` count is not recorded as achieved.  Session 158 gets
one final theorem attempt using the complete cell signs; absent a uniform
inequality, HC-36 stops with K35T's tangent `8 -> 7` reduction as its measured
gain.

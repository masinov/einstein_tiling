# Session 98 — HC-14 fixed-successor rosette

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-14`, session 2 of at most 3

**Mode:** on-paper exact boundary mechanism; no experiment

## Candidate mechanism

A polygon with one keyed head and one keyed tail would give every occurrence
one predecessor and successor at a fixed relative isometry.  Choosing an
order-42 rotation appears to force K5C's bounded corridor cycle.

## Result

N17 refutes the mechanism as a state carrier.  Every finite component longer
than two is the orbit of a finite-order rotation.  The successor rotation acts
transitively on the unmarked ring, so an equivariant local decoder can neither
select one delimiter nor assign a nonconstant word.  The same fixed transform
that closes the ring destroys the root/state asymmetry.

External contacts could root the ring, but then they—not the successor key—are
the missing mechanism and require their own complete gapless proof.  The only
remaining bounded variant uses several independently visible docking modes.

## Stop condition

Session 99 tests whether multiple full-arc modes can be exclusive on one
gaplessly tiled boundary.  If not, HC-14's kill fires and K5C closes without a
shape run.  No coordinates, runner, SVG, candidate or artifact were produced.

# Session 25 — W2.C nonnegative rational no-go

**Date:** 2026-07-17  
**Scope:** determine whether positivity strengthens the exact integer
incidence relaxation before introducing a general LP stack.

## Translation-average theorem

The torus translation group permutes placements and is transitive on spatial
positions within each of the six kite sectors. Averaging any nonnegative
rational solution therefore reduces the full incidence system exactly to the
cone generated by legal placements' six-sector profiles. Conversely, profile
coefficients lift to uniform per-profile placement weights. Conic
Carathéodory bounds a compact witness by six profiles.

The producer enumerates linearly independent profile subsets of size at most
six and solves over exact SymPy rationals. The independent verifier expands the
profile weights across all placements and checks coverage one at every torus
cell using Python `Fraction` arithmetic.

## Result

All 742 area-admissible finalist HNFs through index 60 were classified:

- 36 `fractional-obstructed`, exactly the rational-rank/GF(2) cases;
- 706 `fractional-compatible`, each with a verified full-incidence witness;
- zero new exclusions from nonnegativity.

Artifact:
[`theory-w2-layer-c-nonnegative.json`](assets/theory-w2-layer-c-nonnegative.json),
SHA-256 `a38beabcaafca61a91b7ad9c507a583868ae51bc8e5c89b5acc8dce8ffaeee65`.

## Interpretation

The surviving obstruction is genuinely binary exact-cover combinatorics.
Neither integer cokernels, odd torsion, nor ordinary LP positivity distinguishes
the 706 survivors. No fractional solution is evidence of a tiling. W2 should
now seek family-level binary certificates or advance to nonabelian holonomy.

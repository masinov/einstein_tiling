# Session 131 — rigid-hook cone and K17S edge cases

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-26`, session 1 of 3

**Mode:** on-paper theorem derivation; no experiment

## Authorization and terminal outcomes

The user authorized one theorem-only K16W elimination checkpoint after the
independent HC-24/HC-25 audit.  Exactly three outcomes are admitted: a full
exact simple K16W witness, a scoped K16B incompatibility theorem, or a freeze
at K16W followed by a proposed non-right-guard pivot.  Partial angle analysis
does not count as success.

The audit additionally requires the bridge-independent rigid hooks to be
treated first and requires explicit handling of reflection and an identically
satisfied closure line.

## Result

K18H gives a necessary-and-sufficient translation criterion for one rigid
`v,w,u` hook inside the `v`-by-`u` lens: all six pair differences must obey
twelve two-sided coordinate-span bounds.  Every K17S spine supplies one such
solution for `w=b` and one for `w=c`.

The cone is not itself contradictory.  The exact unequal-lens control

```text
(u,v,w)=(1001/1000,1,1),       r=(3/5,4/5)
```

satisfies it, with horizontal span `7*sqrt(2)/10<1` and vertical span `4/5`.

N35 proves that K17S's closure line cannot be identically satisfied: vanishing
of both line coefficients forces `w_8=0`, directly contradicting the strict
`k=8` containment bounds.  K18R treats the opposite global turn sign by exact
complex conjugation and Euclidean reflection.

## Decision

The requested cheap hook no-go does not exist.  HC-26 remains open for at most
two sessions, which must couple the two exact hook cones through the bridge
units and closure.  The checkpoint trichotomy is unchanged.

## Verification

- All hook formulas were expanded directly from two `3*pi/4` turns.
- The finite-set span criterion proves K18H in both directions.
- The exact control inequalities reduce to strict integer comparisons after
  squaring; no decimal approximation is used.
- The determinant in N35 is `-(u^2+v^2)`, nonzero for positive guard legs.
- Reflection preserves lengths, strict containment and segment intersections.
- No research command or generated artifact was produced.
- Artifact roots remain byte-identical to the HC-26 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.


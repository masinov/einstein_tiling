# Session 195 — ordinary vertices cannot couple the three rails

## Done

- Defined the complete ordinary-sector-star realization family: arbitrary
  binary state-dependent active angles, arbitrary fixed passive sectors, and
  unary/pairwise rooted boundary-germ checks, but no T-junction, overlap,
  hidden star state, or larger-radius exclusion.
- Proved K69A: the family cannot realize the exact ternary even-parity
  relation.
- Applied it to the exact K68V AHI vertex language to close ordinary
  multi-participant vertices as the boundary-active joint coupler.
- Reduced every remaining carrier-local realization to a visible auxiliary
  star state, a non-ordinary contact hyperedge, or a proved larger-radius
  all-tilings exclusion.

## Verified

- Every unary and binary projection of `{000,011,101,110}` is full, so no
  unary or pairwise boundary check may reject a bit combination.
- Subtracting the four accepted angle-sum equations gives
  `d_B+d_C=d_A+d_C=d_A+d_B=0`, hence all three differences vanish.
- The proof is unchanged by fixed guards, nonconsecutive active sectors,
  rooted reflection labels, or nonconvex geometry away from the vertex.
- The scope aligns exactly with N5/N6: a changing hidden completion is not a
  counterexample but must supply at least four product-box states in the
  direct auxiliary model.

## Failures / boundaries

- The theorem does not exclude T-junctions, side-subdivision words,
  multi-arc cavities, carrier--verifier fusion, or a genuine larger-radius
  whole-plane theorem.
- It does not construct the required non-ordinary contact hyperedge.
- No novelty claim is made for the projection or linear-angle argument.

## Open

Classify the full local closure of the minimal host-side subdivision/fusion
interface and decide whether one connected unmarked polygon can realize the
AHI parity hyperedge without an odd contact word or periodic fault tiling.

No experiment, enumeration, solver, polygon, or candidate was opened.

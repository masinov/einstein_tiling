# Session 174 — periodic-scaffold obstruction

## Experiment pre-registration

### Proposition

Decide whether all 31 source addresses and all 44 macro-internal contacts
embed in one translation-periodic, three-orientation rhombus scaffold after
forgetting macro ownership and `S/M/L` roles.  If they do, derive the exact
period lattice and a periodic tiling certificate for the complete
radius-zero/immediate-contact image.

### Prior art and non-redundancy

The source constructs isometric cells from three line families but does not
state or serialize the common-rhombus color-erasure scaffold obtained in
session 172.  Session 173 proves that a binary handedness compiler is too
small and finds that every internal contact changes diagonal axis.  The
present proposition tests the resulting three-axis structure exactly; it is
not a search over polygons or patches.

### Outcome decisions

- A periodic embedding yields a scoped impossibility theorem: no carrier
  whose decoded state is a function only of one rhombus pose or its complete
  immediate uncolored contact star can force the Sturmian source, because
  that local image contains the certified periodic scaffold.
- Failure yields an exact finite incompatibility certificate and keeps a
  one-cell or immediate-star compiler viable; its unmatched source contacts
  become the construction constraints.

### Stop rule and finite justification

Use only the fixed 31 addresses and 44 internal contacts.  Solve exact
integer-affine congruences for a periodic axis function and verify every
serialized component and contact once.  Do not enlarge a patch radius or
launch a polygon search.

### Human checkpoint

`HC-2026-07-27-42`, session distance three from the recorded baseline.  This
is the final finite diagnostic needed to choose between a support-local
compiler and a genuinely contextual carrier; it directly establishes a
construction route or a family-level obstruction.

## Results

The complete affine-modulo-three ansatz was decided.  Exactly three affine
laws define involutive perfect matchings on the whole triangular frame, and
they are the three constant-axis matchings.  Neither 30-cell large template
is contained in any of them; the small two-cell `M` template is contained in
one.  Hence no affine axis law of the preregistered form is the erased source
scaffold.

## Verification

`ahi-periodic-scaffold.json` records all 81 affine coefficient cases and is
cold-rebuilt by `verify_sturmian_periodic_scaffold.py`.  The result uses only
integer residues and exact triangle-partner incidence.

## Decision

The proposed affine periodic-scaffold obstruction fails: no common affine
scaffold exists.  This keeps a genuinely contextual immediate-star compiler
viable and forbids treating diagonal orientation as an affine lattice color.
No claim is made about non-affine periodic matchings.

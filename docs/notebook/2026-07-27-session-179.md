# Session 179 — transcribe the published local interchangeable pair

## Experiment pre-registration

### Proposition

Transcribe every physical source-tile placement in Akiyama--Hamada--Ito
Figure 45 (`Example1.pdf`, page 5) from the pinned vector source.  Normalize
each outline to the exact triangular lattice and identify it against the
already cold-verified `large_A`, `large_B`, and singleton `M` supports.
Determine exactly which two displayed assemblies have the same support and
whether their source-tile multisets absorb the `M+S` versus `L+S` mismatch of
K54R.

### Prior art and non-redundancy

The paper states that Figure 45 contains local interchangeable pairs but does
not serialize their placements.  K54R independently finds a smallest
17-rhombus equal-support relation that is not itself a legal source regrouping.
This run transcribes the authors' source-native regrouping rather than
searching a new patch space.

### Outcome decisions

- A same-support pair in the physical `15,15,1` language becomes the fixed
  inverse-retiling kernel.  The next obligation is an exact-cover/grouping
  theorem for occurrences of that kernel, with unrestricted periodicity and
  decoder gates applied before any candidate claim.
- If Figure 45 concerns only auxiliary overlapping triangle patches or cannot
  be identified with the physical atlas, record that category boundary and do
  not infer a physical common-support compiler from the picture.

### Stop rule and finite justification

Read only the fixed page-5 vector objects.  Resolve their finite SVG transform
tree, snap only after identifying the exact six lattice directions and common
scale, and require each snapped outline to match a pinned atlas support under
a lattice isometry.  Do not enumerate placements, larger patches, boundary
words, or approximate candidate polygons.

### Human checkpoint

`HC-2026-07-27-44`, session distance two.  This is direct source
reconstruction for K52E/K53E.

## Results

Figure 45 contains 32 physical source-tile outlines, partitioned into four
assemblies.  Exact normalization gives two same-support pairs:

```text
pair 1: 3 large_A + 6 singleton_M = 51 rhombi
pair 2: 2 large_A + 1 large_B + 4 singleton_M = 49 rhombi
```

Every assembly is an interior-disjoint topological disk with a 30-edge
primitive boundary.  The two decompositions in each pair are, however,
related by a full lattice isometry of their common support.  Thus Figure 45
supplies a contextual flip relative to a surrounding/rooted Sturmian frame,
not two intrinsic states of an isolated unmarked carrier.

## Verification

The cold verifier re-extracts page 5 from the pinned arXiv source archive,
resolves the complete SVG transform tree, identifies all 32 outlines against
the exact source atlas, reconstructs the four contact graphs, and compares
supports and typed decompositions under all twelve lattice isometries.

```text
data/sturmian-source/ahi-interchangeable-pairs.json
sha256 3be14d425f625a49006ca5e911a79ab6fdcd1b57ab8dd436155f54a4e422b9c7
```

## Decision

Retain both pairs as exact rooted/contextual compiler kernels.  Do not promote
either bare support as a binary carrier state: support symmetry exchanges the
two decompositions.  Any use must first exhibit the external contact context
that fixes the root and prove it on the full local closure.

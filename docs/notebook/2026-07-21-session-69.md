# Session 69 — corrected common-support geometry

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-04`, session 3 of at most 3

**Mode:** primary-source geometry and on-paper proof only; no experiment,
runner, atlas enumeration, shape construction, or generated repository asset

## Question

Does normalized equidistancing give the actual optimized templates one common
primitive support, and if so what are the correct raw address counts? The
session was not authorized to infer or enumerate the complete limiting
language.

## Exact centroid derivation

In cabinet coordinates, lines `a=A`, `b=B`, `c=C` are
`x+y=-A`, `x=B`, `y=C`. Their triangle has centroid

`g(B,C;A)=((2B-A-C)/3,(2C-A-B)/3)`.

If consecutive `b,c` gaps are `p,q`, the four centroids defining an isometric
cell form a parallelogram with edge vectors

`p(2,-1)/3` and `q(-1,2)/3`.

The fixed linear map to isometric coordinates sends the corresponding unit
vectors to equal-length vectors at 120 degrees. Each gap in
`SL(kappa,beta)` is `kappa` or `kappa+1`; after scaling by `1/kappa`, both
tend to one. All `S/M/L` cells and their cyclic orientations therefore tend
to one nondegenerate `60/120` rhombus in a periodic complex.

## ST-M1.G0

The source splits `S` and `L` along the diagonal not parallel to the omitted
line family. In the limiting rhombus this is the short diagonal, producing
two equilateral triangles. Split each `M` along the corresponding diagonal.
All constituent supports are then congruent.

The actual large composition becomes

`12 + 2*6 + 6 = 30`

triangles; the small `M` becomes two. The corrected template counts are
therefore `30,30,2`, or 62 raw geometric addresses. Splitting preserves
supports, and positive limiting edge adjacencies preserve connectivity of the
source's disk-like templates.

## Remaining language obstruction

G0 closes only support geometry. Under equidistancing, tiny triangles of the
generating line arrangement collapse to vertices. The limiting triangular
complex may therefore contain point contacts not present as finite-kappa edge
contacts. A colored system must still transport:

- bent SAB continuation;
- complete vertex stars, including new point contacts;
- the two large geometries and reflected occurrence;
- paired halves of every split `M`;
- a total all-tilings decoder excluding spurious all-`M` recombinations.

This is the separate open lemma ST-M1.L0. Until it closes, the 62 addresses
are geometric cells, not a proved safe symbolic alphabet, and K1 remains
blocked.

## Decision and stop

Retain G0 as a proof draft and sharpen E-infinity from a geometry question to
the exact language-transport lemma L0. No atlas was enumerated and no collar
radius was chosen. This is the third session under HC-04; research stops for
human review after verification and commit.

No `EXPERIMENTS.md` entry is added. Artifact roots are untouched.

## Verification

`git diff --check` and checkpoint JSON parsing pass. The targeted governance
suite passes `4/4`; the complete fast suite passes `222/222` with 17 slow
tests deselected in 144.42 s. Artifact roots remain byte-for-byte at the
HC-04 baselines: `6,756,125,300` bytes under `docs/notebook/assets` and
`1,793,649,957` bytes under `data`.

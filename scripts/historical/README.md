# Coupled historical commands

These commands remain executable because regression controls load them or
their frozen artifacts directly. They belong to closed research systems and
are not public construction APIs. Reusable exact logic has been moved into
`src/einstein`; remaining orchestration stays here to preserve provenance.

The 92 uncoupled one-off runners are separately hash-pinned under
`scripts/archive/`.

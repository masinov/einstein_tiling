# Session 164 — HC-38 clean-spoke topology classification

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-38`, session 3 of 3

## Objective

Convert N44's K28W failure into an exhaustive topology result and a reusable
pre-geometry synthesis filter. Determine whether another edge-minimal
full-side/half-turn carrier exists between the old equal-spoke K10B and the
unequal-spoke K16B family.

## Result

Theory note 50 proves:

- K42P: one boundary side serving as both an outgoing port of `X` and an
  incoming port of `Y` forces their lengths equal; unequal ports require a
  bridge vertex and minimally the two-side word `u,v`.
- K42M: under the fixed shield order and half-turn pairing, the only
  edge-minimal words are exactly K10B (15 edges) for `u=v` and K16B (19
  edges) for `u!=v`, up to reversal.
- N46: no third minimal clean-spoke half-turn family exists. K28G was not a
  fresh topology; its unchanged internal connectors force it into the equal
  branch.

The resulting six-step port-incidence compiler is a conservative candidate-
generation filter: it rejects impossible topologies by equality propagation
and inserts every forced bridge before geometry. It does not enumerate
polygons or claim methodological novelty.

## Verification

The selected first-half role order has exactly two constrained consecutive
code gaps, `A--B` and `B--C`. For unequal endpoint ports, each needs one new
bridge vertex. Central pairing forces the two partner bridges, accounting for
the exact `15+4=19` edge count. The minimal first half

```text
A,u,v,B,u,v,C,u
```

determines the complete K16B word uniquely. The equal/unequal dichotomy is
exhaustive.

## Decision

HC-38's theorem allowance is exhausted. Future theory may not introduce a
new thin-lens family by changing weights, angles or outer lens lengths. A
genuinely new carrier must alter at least one of full-side contacts `F`, role
order `O`, half-turn docking `J`, or the single-guard topology `S`.

The recommended HC-39 question is the bounded group/geometric audit of `J`:
can a reflection-based two-copy shield preserve the binary host state and
give a non-thin carrier, or does it create a new symmetry no-go? Partial
contacts (`F`) remain secondary because they require a larger complete
T-junction language.

The HC-38 tangent processes remain governed by session 162 and continue
independently. No solver status is promoted in this session.

The mutable live manifest calls `code_version()` at observation time, so
parallel theory commits can make that diagnostic field show a later or
`-dirty` worktree. It is not launch provenance. The launch revision is
`b38a4e0`; the solved bytes are independently fixed by the six SHA-256 values
in the immutable `fa0c190` formula manifest. Final interpretation must use
those two records, not the mutable live field.

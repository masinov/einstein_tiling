# Session 32 — factor the A4 obstruction

**Date:** 2026-07-18
**Scope:** replace the opaque A4 class signature by exact `V4 semidirect C3`
coordinates, derive the geometric twist reduction, and falsify the resulting
signature on the index-55 W1 frontier.

## Exact semidirect factorization

The canonical permutation table is now independently represented as

`A4 = GF(2)^2 semidirect C3`,

with `M(x,y)=(y,x+y)`. Exhaustive tests reproduce all 144 products, all
inverses, all 48 commuting pairs, and all 1,728 instances of the Layer-D edge
equation. The equation splits into a C3 affine transport and a two-bit twisted
linear transport; no numerical or heuristic step enters.

For map 7:

| generator vectors | `(0,1)` | `(1,0)` | `(1,-1)` | `(1,1)` | `(1,-2)` | `(2,-1)` |
|---|---:|---:|---:|---:|---:|---:|
| C3 | 1 | 2 | 1 | 0 | 0 | 0 |
| V4 | 0 | 0 | 0 | 0 | `e1` | `e2` |

The C3 row is exactly `chi(x,y)=2x+y mod 3`. Both period translations of any
center HNF have character zero. Thus an exact cover can only induce V4-valued
torus twists, reducing 48 A4 commuting pairs to 16 ordered V4 pairs (T2.D4).
The full index-50 certificates remain stronger because they already excluded
all 48.

The previously observed 16-map signature is exact in these coordinates: the
three tail labels have C3 value zero and three distinct V4 values. Artifact:
`assets/theory-w2-layer-d-a4-factor.json`, SHA-256
`81c2e1720f9874536c194968a62c7437b839042b29ca258e3f705ab0e78f6b4b`.

## Index-55 falsification design

W1 leaves 21 of the 72 index-55 HNFs. They form four D6 orbits of sizes
`6,3,6,6`. Crossing them with the 16 distinct-V4-tail maps gives 336 logical
pairs; the exact diagonal action is free and reduces them to 28 representatives.
The experiment tests every commuting twist, not only the 16 now known to be
necessary, so a surviving signature receives the same stronger interpretation
as at index 50. UNSAT remains search-only until independent proof production.

## Index-55 falsification result

The signature survives unchanged. All 28 diagonal pair-orbit representatives
are UNSAT for all 48 commuting A4 twists. Expanding the exact D6 action gives
all 336 HNF/map pairs, so each of the 21 W1-surviving HNFs is obstructed by all
16 distinct-V4-tail map classes. There are no relaxed-CSP SAT witnesses in
this signature slice.

Artifact: `assets/theory-w2-layer-d-a4-signature-index55.json`, SHA-256
`4c988214092f7fde1a7c980afdb37baff41ca75bacb023531e81c0ac4ecaa078`.
Its dependency and source hashes were regenerated and checked. This result is
not promoted to T2.D2-55 until the theorem-sufficient 21 x 16 V4-twist CNFs
have independently replayable DRAT certificates.

## Proof promotion

The proof gate is complete. Map 7 was fixed on all 21 HNFs and each of the 16
T2.D4-relevant V4 twist pairs was encoded directly, giving 336 canonical CNFs.
Glucose 4 produced 336 UNSAT proofs; `drat-trim` verified every raw proof,
extracted and reverified every core, and the producer checked each core as a
multiplicity-respecting subset of its generated CNF. A separate cold run then
regenerated and replayed 336/336 stored certificates.

Manifest: `assets/theory-w2-layer-d-a4-proof-index55.json`, SHA-256
`fa4a47ff64c59360aaa91be550507f315c8b71c1491e101885b989775277e5fc`.
Compressed certificate bytes: `3,021,269,794` in 672 ignored payload files.

The index-55 shell is therefore exactly `51 W1 + 21 A4`, and indices 51--54
fail the area equation. T2.D2-55 is promoted: no grid-aligned finalist exact
cover exists on an HNF torus of index at most 55. O1, O3, and O5 remain open.

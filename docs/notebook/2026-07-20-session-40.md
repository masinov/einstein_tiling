# Session 40 — exact Turtle identity correction

## Trigger

The user recognized that the n=10 shape historically promoted as the E1
“finalist” looked like the published Turtle and supplied James Smith's
*Turtles, Hats and Spectres* (arXiv:2403.01911) for comparison.

## Exact comparison

Visual similarity was not used as the identity test. The source bundle of the
primary Hat/Turtle paper (Smith--Myers--Kaplan--Goodman-Strauss,
arXiv:2303.10798) defines Turtle as `tileB` with the exact hex-basis outline

```text
(0,0), (-2,1), (-2,0), (-3,0), (-2,-2), (2,-4), (3,-3),
(4,-4), (5,-4), (4,-2), (2,-1), (2,0), (1,1).
```

The repository's exact `cells_in_polygon` recovers ten kites and doubled area
40. Applying the production `canonical_form` gives

```text
010001010104010502f002f1030b030c04fa04fb
```

byte-for-byte equal to n=10 candidate 2. `tests/test_turtle.py` now pins both
the ten-cell decomposition and canonical-key equality. The Hat and Turtle
keys/outlines are registered in `src/einstein/e1_candidates.py` so promotion
can classify known polykites before novelty ranking.

## Correction and preserved evidence

The shape is the known ten-kite Turtle, `Tile(sqrt(3),1)`, not a new candidate.
The failure was promotion-time known-shape deduplication; enumeration,
canonicalization, patches, and finite certificates were not falsified.

The Turtle's tilability and nonperiodicity are already established. The 2024
paper supplies a Bricks-and-Mortar recursive construction and an Ammann-bar
proof: a hypothetical periodic fundamental domain forces rational `q=k/n` to
satisfy `q(1-q)=1/5`, whose roots `(5±sqrt(5))/10` are irrational.

All `e1-finalist-*`, `theory-w1-finalist-*`, and related artifact names are
retained as stable provenance paths. “Finalist” is now explicitly a legacy
alias for the Turtle control. Their finite computations remain useful as
independent certificate-method validation, but not as evidence for a novel
monotile. ERR-003 and D-0048 govern this interpretation; historical notebooks
remain unchanged as an audit trail.

## Search consequence

The next discovery batch must exclude registered Hat and Turtle keys before
promotion. The bounded next set remains the 29 n=14/n=15 depth-3 survivors.
Additional W1/W2 shells for Turtle are optional validation rather than a
candidate-decision priority.

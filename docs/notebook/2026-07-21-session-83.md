# Session 83 — sector-separated six-valent vertex lift

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-09`, session 2 of at most 3

**Mode:** on-paper vertex-consistency analysis only; no polygon, contact
census, search, drawing, experiment or generated artifact

## Question

Can K2C's corner potentials coexist at a geometric vertex where six carriers
meet?

## Common-bit warning

Identifying all six participant sectors with one vertex bit would force the
two directed half-mode parities across every edge to agree. K1P's arbitrary
center-state code does not prove that compatibility. A common vertex bit is
therefore not available without a new source-code theorem.

Conditionally, if parity matching held, zero face holonomy and simple
connectivity would integrate the edge parities to one global vertex potential.
That simplification is recorded but not assumed.

## K2V

Retaining one bit for each incident face sector always works. Each triangular
carrier independently integrates its own even codeword, and the shared
geometric vertex records the six participant bits in cyclic order. No cross-
face equality is introduced. This proves a symbolic sector-separated lift for
every K1P configuration.

The raw base junction alphabet has at most `2^6=64` sector words; source
vertex legality restricts it further. This is a finiteness statement, not an
atlas census.

## Decision

K2C survives the shared-vertex test only in sector-aware form. The final
HC-09 session must state a noncircular geometric junction contract: which
locally visible feature distinguishes participant sectors and how the same
fixed boundary transports it along each side. If that statement merely
relabels colors, the kill condition closes the route.

No `EXPERIMENTS.md` entry is added.

## Verification

- common-bit identification was checked on both directed halves of one edge;
- independent face integration proves K2V without a global gauge choice;
- the 64-word bound is only the six-bit raw base bound, not a claimed source
  atlas cardinality;
- `git diff --check` and governance tests passed;
- no research command or generated artifact was created.

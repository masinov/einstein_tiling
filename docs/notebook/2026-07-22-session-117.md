# Session 117 — K10B root audit and HC-20 stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-20`, session 3 of 3

**Mode:** on-paper synchronization audit; no experiment

## Proposition

Apply K11S to the fixed K10B word, correcting any endpoint assumption before
deciding whether coordinates may resume.

## Correction found first

The shared spine divides a full neighborhood only at nonterminal vertices.
Its terminal `A` endpoints lie at the right-angle lens corners, where the two
carrier sectors sum to `pi/2`, not `2*pi`. ERR-009 corrects N26/N27 and blocks
the tempting but false proof that paired `A` is reflex at both endpoints.
K11S itself is unaffected.

## Result

N28 proves the unique `H=7` side is not atomic. The half-turn shield requires
the root cover `[H]`, while the two selected host states require `[A,B,C]` and
`[A,C,B]`; all three are exact because `7=1+2+4`.

No other K10B side has the complete certificate K11S requires. Paired `B,C`
have the useful reflex polarity but admit numerical subdivision identities;
the terminal `A` needs a separate vertex-alignment proof; and repeated `d`
sides lack a unique rooted role. None of these facts proves an alternative
contact legal, but they prevent an atomicity claim without the missing finite
cover table.

## Decision

HC-20 closes. Auxiliary duplication is controllable in general under K11S,
but the fixed K10B skeleton has no proved synchronizing root. K10W remains
frozen because neither a complete atomic-root certificate nor its required
exact coordinates and two placement tables exist. No polygon, SVG,
experiment or candidate follows.

## Verification

- Terminal lens vectors `(1,1)` and `(1,-1)` are perpendicular.
- The three `H` cover words are required by K10B/K9A, not guessed contacts.
- `git diff --check`: clean.
- Full fast suite: `222 passed, 17 deselected`.
- Artifact roots remain `docs/notebook/assets = 6,756,125,300` bytes and
  `data = 1,809,060,890` bytes.

# Session 124 — fixed K13F role audit and square lens

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-23`, session 1 of 3

**Mode:** on-paper exact geometry admission; no experiment

## Proposition

Redo role recognition for `(1,4,6,11;d=12)`, close the declared side-cover
tables, and put the K10B lens in exact coordinates before attempting closure.

## Result

The length classes are distinct but not sufficient for recognition. The two
copies of `A,B,C` retain the corrected intended/auxiliary endpoint contexts;
`H` is unique; and the eight `d` sides need ordered context. All full-side
coefficient partitions of `A,B,C,H,d` over `{1,4,6,11,12}` are listed in
K14R. In particular, `B=4A`, `C=B+2A`, and
`d=H+A=2C=3B` create new non-host contact burdens.

The equal-leg right-angle guard and its half-turn form a square of side `d`.
Using its two sides from `R` as orthonormal axes, every spine point must have
both coordinates in `[0,d]`. The spine begins with lengths `1,12` and turns
by `3pi/4` at their `pi/4` interior angle.

## Decision

Session 125 tests this two-edge prefix and the central `H` edge against the
square before any closure coordinates. A violated necessary inequality closes
the fixed instance. No alternative tuple, `d`, word or computation is
admitted.

## Verification

- The coefficient tables solve `r+4p+6q=t` exhaustively for each target.
- A right isosceles guard triangle and its half-turn give the square (3.2).
- No coordinate fitting or artifact was generated.

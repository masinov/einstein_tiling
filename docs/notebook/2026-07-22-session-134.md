# Session 134 — K20L repair and K16W exact-decision pre-registration

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-27`, session 1 of 3

## Experiment pre-registration

### Proposition

Decide the satisfiability of one fixed normalized existential-polynomial
sentence equivalent to K16W: positive unequal guard legs with `u=1`, positive
`a,b,c`, three unit directions represented by rational tangent-half-angle
parameters, all eight strict K17S first-half containment bounds, exact central
host closure, nondegenerate bridge angles, and strict disjointness of every
nonadjacent pair among the seventeen spine segments.  Guard-edge intersections
are already excluded by strict open-rectangle containment.

### Prior art and non-redundancy

Jovanovic and de Moura, *Solving Non-Linear Arithmetic* (IJCAR 2012), give
NLSAT as a decision procedure for quantifier-free polynomial real arithmetic;
the official Z3 programming guide identifies `SolverFor("QF_NRA")` as its
self-contained implementation.  This run claims no solver or decision-method
novelty.  Its non-redundant purpose is to decide the repository's fully
specified K16W carrier obligation, for which the audited literature contains
neither a witness nor an incompatibility result.  The exact formula and cold
geometric verification, not the use of NLSAT, are the research object.

### Outcome decisions

- If SAT yields a model that a separate exact-arithmetic verifier confirms
  against the original coordinates, closure and all segment intersections,
  record one K16W witness and reopen only the carrier-geometry branch; do not
  call it a monotile or candidate before placement and all-tilings proofs.
- If UNSAT comes with a replayable exact certificate that an independent
  checker validates, close K16B.  A bare solver UNSAT without that certificate
  is recorded only as solver evidence and does not become a theorem.
- If the solver returns unknown, exceeds the fixed stop, or produces a model
  that the cold verifier cannot certify exactly, retain the K16W freeze and
  propose the already-selected symbolic `gamma!=pi/2` family checkpoint.

### Stop rule and finite justification

Run one syntactically fixed QF_NRA instance, with one variable ordering and an
eight-hour wall-clock / 96-GiB resident-memory ceiling.  Unit-circle variables
are rationalized once; all 120 nonadjacent spine pairs receive their complete
exact nonintersection predicate (equivalently, strict separation of the two
compact segments) before launch.  The instance is a finite
decision problem, not a radius or sampling prefix.  There is no second seed,
ordering, relaxed simplicity formula, larger time budget or follow-up search
inside HC-27; any nonterminal outcome fires the stated freeze.

### Human checkpoint

This is session `+1` under `HC-2026-07-22-27`.  The checkpoint allows at most
three sessions and 1 GiB of new governed artifacts.  Baseline roots are
`docs/notebook/assets=6756125300` and `data=1809060890` bytes; the planned SMT
formula, log and result manifest are small.  The user explicitly authorized
continuation after the HC-26 audit and required ERR-010 as the first repair.

## Results

ERR-010 repairs K20L with the exact unit vector
`(544/545,33/545)` and retracts the false session-133 verification identity.
The experiment gate passed. K21Q now serializes the complete normalized
formula: 13 base constraints, 32 scalar containment bounds, one closure
equation and 120 exact nonadjacent-segment predicates. A separate verifier
reconstructs rational models over `Q(sqrt(2))`. The research run has not yet
returned a verdict.

The first foreground launch was interrupted after approximately six minutes
solely to honor the user's request that the eight-hour job survive in a
`screen` session. No formula, variable ordering, solver option or budget was
changed. The identical gated command was relaunched at 23:04 Europe/Madrid in

```text
screen:  k16w_hc27
log:     logs/k16w-hc27.screen.log
formula sha256:
b3e4c2acf34e3e5fe78feb3ce5b79c6583af77649261f2ee42c805573e6ad301
```

The host screen registry reports the session detached and alive. This is an
infrastructure migration of the single preregistered configuration, not an
alternate solver experiment.

## Verification

- `544^2+33^2=545^2`.
- Corrected hook spans and their exact comparisons are serialized in theory
  note 32 and ERR-010.
- `scripts/check_experiment_gate.py` reports `PASS` before the runner is
  written.
- Formula construction tests pin 18 spine vertices, 120 nonadjacent pairs and
  166 top-level constraints.

## Decision

Pending the result of the running gated screen job.

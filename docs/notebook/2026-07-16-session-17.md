# Session 17 — 2026-07-16 — First high-priority new-shape finalist

## Question answered

Yes, there are genuinely new candidates. More precisely:

- 9,728 shapes have verified three-corona chains;
- the complete smallest batch contains two n=10 and eight n=12 shapes;
- after A3, extended exact A1 and A4, exactly one of those ten is a
  high-priority finalist.

This statement does not claim a new einstein.

## Complete smallest-batch A3 screen

Every test was pose-free, so UNSAT is an exact grid-aligned disk-cover
refutation rather than a seed-pose failure.

| outcome | count | detail |
|---|---:|---|
| refuted | 1 | n=10 candidate 1 grows at r²=200, then is UNSAT at r²=800 |
| grows through r²=12,800 | 9 | 2,012–2,404 independently verified tiles |
| unknown | 0 | no conflict-budget exhaustions |

Artifacts:

- `a3-small-candidate-results.json`
- `a3-small-candidate-patches.svg`

## Exact periodicity extension

The production sweep stopped at torus index 12. The crystal-like large
patches justified an exact escalation.

- all eight n=12 shapes have verified torus covers at index 16;
- both n=10 shapes have no torus cover through the compiled engine's index 21;
- the surviving n=10 shape was then swept by exact CaDiCaL torus cover through
  index 100;
- all 20 arithmetically admissible indices completed, with no exhaustion and
  no certificate.

Thus the eight n=12 shapes are periodic budget escapes, not candidates.

## The finalist

Identity:

- n=10 candidate 2;
- key `010001010104010502f002f1030b030c04fa04fb`.

Escalated evidence:

| check | result |
|---|---|
| A1 exact torus | no certificate through index 100; zero exhausted indices |
| A3 pose-free disk | r²=50,000, 90,714 required kite cells, 9,239 tiles |
| A3 SAT instance | 184,704 variables, 17,519,322 clauses, 266.32 s solve |
| A4 at 2,404 anchors | rank 4; numerical symmetry vote 2 |
| A4 at 9,239 anchors | rank 4; symmetry 6; quasicrystal-candidate signal |

The larger A4 run uses a deterministic matched random null and the calibrated
2048² fingerprint pipeline. The full patch and spectrum were visually
inspected.

Artifacts:

- `e1-finalist-results.json`
- `e1-finalist-patch.svg`
- `e1-finalist-spectrum.png`
- `scripts/run_e1_finalist.py`

## Interpretation

This is the first shape discovered by the blind n≤16 funnel that deserves
focused follow-up. It has deep local growth, a large exact disk patch, no
small or medium periodic quotient, and stable rank-4 diffraction.

It is not yet proven to tile the plane, and it is not proven aperiodic.
The next work is:

1. generate independent r²=50,000+ solutions using different SAT phases;
2. search for periodic constructions beyond quotient index 100 and with
   structure-guided unit-cell hypotheses;
3. run blind A6 hierarchy mining on the new patch;
4. test whether the rank-4 module and sixfold peak set persist under still
   larger patches and independent boundary conditions.

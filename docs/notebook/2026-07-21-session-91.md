# Session 91 — HC-12 substrate identity and audit admission

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-12`, session 1 of 3

**Mode:** read-only source and exact-geometry audit; no experiment

## Result

The K3F flag carrier is the exact primitive kite implemented by
`substrate/kitegrid.py`, after multiplying the K3F normalization by
`sqrt(3)`.  Both have ordered angles `60,90,120,90`; their ordered side lists
become `sqrt(3),1,1,sqrt(3)`.  Future flag-level geometry can therefore reuse
the exact integer substrate and `D6` symmetry machinery.

HC-12 also fixes the terminology that controls B0: rule support, decoder
radius and geometric visibility are separate.  The primary-source audit has
been admitted with three catalogued sources and no computational census.

## Verification

- compared the K3F metric derivation with the source code's documented scale,
  vertex order and exact norm;
- verified that the polykite `n<=24` prior-art horizon applies only when a
  proposed support remains a finite union of primitive kite cells;
- catalogued Hu--Lin, Kari--Moutot and Jeandel--Rao with claim-specific review
  status;
- no research runner, shape search or generated artifact was used.

## Next

Read the relevant theorems in context.  Decide separately whether immediate
binary `2x2` plaquette rules can be aperiodic and whether larger binary
rectangular supports can simulate the source without spurious configurations.

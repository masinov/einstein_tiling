# Session 123 — infinite passing weight family and HC-22 close

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-22`, session 3 of 3

**Mode:** on-paper arithmetic family theorem; no experiment

## Proposition

Prove a reusable family satisfying K13A, or close the multiplexed-host
arithmetic class without a triple search.

## Result

K13F proves that for every integer `n>=4`,

```text
(a,b,c,h)=(1,n,n+2,2n+3)
```

accepts exactly `ABC,ACB` among code-only host covers. In both semigroup
equations, writing `k=p+q` leaves only `k=2`: U1 forces `p=q=1`, while U2
would require the impossible odd offset `2n+3` among
`2n,2n+2,2n+4`.

The smallest example is `(1,4,6,11)`. Under the separate cover-side alignment
hypothesis, `d=3n>h` excludes complete `d` sides from an `H` partition and
leaves the arithmetic cover words `[H],ABC,ACB`.

## Decision

HC-22 closes positively as a reusable arithmetic boundary-design theorem.
It removes the N29 repetition trap for a changed length family, not for the
original `(1,2,4)` instance. No polygon, coordinates, patch, sliding
exclusion, total decoder, tiling, aperiodicity result or candidate follows.

## Verification

- For `n>=4`, `3n>2n+3` and `n+2<2n+2` exhaust U1 outside `k=2`.
- For U2, `k=2` gives only `2n,2n+2,2n+4`, never `2n+3`.
- Full fast suite: `222 passed, 17 deselected`.
- Artifact roots remain `docs/notebook/assets = 6,756,125,300` bytes and
  `data = 1,809,060,890` bytes.

# Session 154 — ERR-014 host-process visibility correction

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-35`, session 2 of 3

## Done

Before launching HC-35, ran host-level screen and process diagnostics.  They
contradict session 152's sandbox-only diagnosis.  The original
`k16w_hc34` screen is detached and its full process chain is alive.  S1-- has
been continuously inside Z3 under `/usr/bin/timeout 10800`; the two other
first-batch logs remain the genuine pre-solver textual-drift exceptions.

Recorded ERR-014 and D-0176.  HC-35 is cancelled before any research service,
viewport or result manifest is started.  The prepared launcher remains tested
infrastructure but has no experimental output.

## Verified

The host process tree contains one and only one K16W solver process, owned by
the original HC-34 screen and timeout.  Its parent is the session-151 gated
supervisor.  No `run_k16w_hc35.py` process or `k16w-hc35-*` user service
exists.  The host boot time and socket timestamps explain why sandbox
`screen -ls` could label every host session `Dead ???`: the restricted
namespace could not contact them.

The old supervisor's source waits on each sibling and treats nonzero exits as
return codes.  Therefore the two formula-drift exceptions did not cascade
into S1--; the prior causal explanation is withdrawn.

## Failure and evidence boundary

There is still no completed solver verdict at the time of this entry.  Active
CPU time is progress, not evidence.  S1-+ and S2-- remain open because they
never entered the solver.  No cell is retried.

When S1-- terminates, the original supervisor may launch its pre-registered
second batch.  Those new child processes will read the same immutable formulas
through the repaired loader now on disk.  This is continuation of the
original fixed run, not HC-35.

## Decision

Leave `k16w_hc34` undisturbed and monitor it from the host namespace.  Do not
launch HC-35.  Final documentation and any new authorization wait for the
original aggregate manifest.

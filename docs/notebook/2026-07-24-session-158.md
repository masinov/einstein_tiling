# Session 158 — HC-36 root ordering and close

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-36`, session 3 of 3

## Admission

This is the final theorem-only session authorized by D-0178.  It tests whether
the exact signs in K36P can be fixed from the polarity, endpoint and strand
information already proved.  No solver, CAS, numerical sweep, experiment,
coordinate fitting or candidate work is admitted.

The stop is unconditional at the end of this session.  A partial ordering of
roots may be retained as structure, but it counts as HC-36 success only if it
strictly lowers variables, roots, live cells or maximum degree.

## Result

K37O factors the two terminal roots and proves that the plus root is a strict
counterclockwise rotation of the minus root.  K37Q uses the physical long-
strand polarities and positive coordinates of `p_8` to prove
`Q_B>0>Q_C`.  Hence both B and C midline signs may remain fixed or move from
below H to above H as the radical sign changes, but never in reverse.

K37S consequently reduces the possible ordered two-complete-root cell pairs
from nine to six.  This is structural only: it removes no live cell or root,
so it is not counted under D-0178's measurable-success rule.

## Verified

The factorization

```text
Z_epsilon=(A+i*B)*(T+i*epsilon*sqrt(Delta))/R
```

expands term-for-term to K30E.  `T>0` follows from the dot product of two
strictly positive physical vectors.  Rotation invariance turns `Q_B,Q_C` into
dot products of `p_8` with respectively a positive-coordinate and a
negative-coordinate long-strand direction, fixing their strict signs.  No
research command was run.

The first full-suite run correctly failed one stale governance assertion that
still pinned HC-35 (`233 passed, 1 failed, 17 deselected`).  The fixture was
then advanced to the explicitly authorized HC-36 record.  Its targeted tests
pass `4/4`, and the complete repository suite passes
`234 passed, 17 deselected` in 141.96 seconds.  Both governed artifact roots
are byte-identical to the HC-36 baseline:

```text
docs/notebook/assets  6,759,771,649 bytes
data                  1,809,060,890 bytes.
```

## HC-36 disposition

The checkpoint closes with exactly one admitted quantitative reduction:

```text
tangent solver variables       8 -> 7,
tangent geometric variables    7 -> 6.
```

The transverse root count remains two and all six K34Q cells remain open.
No polynomial-degree reduction is claimed.  The checkpoint's three-session
allowance is exhausted; continuation requires a new human decision.

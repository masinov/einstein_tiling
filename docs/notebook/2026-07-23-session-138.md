# Session 138 — HC-29 spoke-crossing theorem

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-29`, session 1 of 3

## Authorization and proposition

The user explicitly authorized HC-29 after ERR-012. Before any solver design,
derive the exact intersection criterion for the nonadjacent first and second
`d` spokes `p_1p_2` and `p_3p_4` in the complete K22S family. Apply it to
both corrected non-right angle branches.

## Decisions

- If containment forces an interior intersection on both branches, close
  K24W by theorem and do not write or launch a decision runner.
- If one or more semialgebraic cells survive, serialize their analytic
  boundaries first; only a later session may preregister a complete
  decomposed exact decision.
- If the intersection criterion itself is inconclusive, record the precise
  residue rather than sampling angles or escalating context.

## Stop and artifact rule

This is a theorem session. No numerical angle/orientation sweep, SMT, CAD,
QE, optimizer, runner, SVG or candidate promotion is admitted. The HC-29
baseline is `docs/notebook/assets=6756513353`, `data=1809060890`; governed
artifacts should remain byte-identical.

## Results

1. **K25X: exact spoke-pair intersection.** For the nonadjacent K22S
   spokes `p_1p_2` and `p_3p_4`, division by the common direction gives the
   unique line-intersection parameters

   ```text
   u=b/(2*d*sin(gamma/2)),   t=1-u.
   ```

   Thus simplicity requires `b>2*d*sin(gamma/2)`; equality is already a
   forbidden nonadjacent endpoint coincidence.
2. **Upper branch.** For `pi/2<=gamma<2*pi/3`, the rhombus diameter is
   `2*d*sin(gamma/2)`. Strict containment of the intervening length-`b` edge
   forces the reverse strict inequality, so the spokes cross.
3. **Lower branch.** For `pi/3<gamma<pi/2`, the corrected K23I window (8.7)
   makes one normalized oblique-coordinate change strictly larger than
   `1/(2*sin(gamma/2))`. Strict containment again forces
   `b<2*d*sin(gamma/2)`, hence the same crossing.
4. **N37 closes K24W.** K23I excludes the interval endpoints. No domain
   survives for the proposed decomposed decision procedure, so no experiment
   or runner was written.

## Exact checks

- Substituting K24C's `b=1/2`, `d=1`, and `sin(gamma/2)=3/5` into K25X gives
  `u=5/12`, `t=7/12`, exactly recovering N36.
- The lower-branch strict comparison uses
  `epsilon<phi/2<gamma`, hence
  `|gamma/2-epsilon|<gamma/2`; no upper-branch form of (8.7) is used.
- The upper branch uses the longer rhombus diagonal because
  `sin(gamma/2)>=cos(gamma/2)` there.
- The reflected global orientation is complex conjugate and has identical
  intersection parameters.

## Artifacts and verification

No research command ran and no governed artifact was created. The proof is
in `docs/theory/34_nonright_spoke_crossing.md`; the ledger records K25X, N37,
and the closure of K24W.

## Decision

HC-29 closes after session 138, one session early. The fixed equal-spoke
non-right rhombic family is impossible by a single forced crossing. This is
a family-level geometric no-go, not a monotile result, and it supplies no new
tile. A future checkpoint must choose a hypothesis outside K22S rather than
reopening its now-empty solver domain.

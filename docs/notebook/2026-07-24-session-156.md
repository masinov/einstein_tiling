# Session 156 — HC-36 tangent closure stratum

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-36`, session 1 of 3

## Admission

This is theorem-only work under D-0178.  It will read the exact K17S/K32R
geometry and derive consequences on paper.  It will not invoke a solver, CAS,
numerical sweep, research runner, coordinate fitting or experiment.  The
first target is the `Delta=0` closure stratum; any retained result must either
eliminate a complete stratum/cell or reduce one of the four predeclared
complexity counts exactly.

## Planned derivation

1. Express the line--circle discriminant geometrically in the physical
   preterminal point `p_8`.
2. Derive the unique tangent terminal direction and its exact half-angle
   chart.
3. Count the variables and degrees of the resulting presentation against the
   deployed K21Q encoding.
4. Test whether the tangent host diagonal forces a named nonadjacent contact;
   if not, record the lower-dimensional stratum without claiming emptiness.

## Result

K35D rewrites the two K32R roots in the physical diagonal frame.  The radical
sign is exactly the sign of `det((v,1),p_8)`.  On `Delta=0`, corrected N42
selects the internal tangent branch: `p_8=lambda*(v,1)`,
`0<lambda<1/2`, and the host is the remaining diagonal segment.

K35T gives an exact lower-dimensional formula.  The unique terminal direction
is `(A/T,B/T)` and the deployed half-angle variable is `B/(T+A)`.  Retaining
all original predicates after this substitution changes the exact cell count
from eight to seven solver real variables (seven to six geometric variables,
with `sqrt_half` retained as the fixed algebraic auxiliary).  No lower degree
or tangent-cell emptiness is claimed.

K35F gives an exact separation control showing that closure, first-quadrant
terminal pose, strict `p_8` containment and H-east can all hold for both
transverse roots.  Thus a one-root result must use additional full-spine data.

## Verified

All identities were expanded in the orthogonal basis
`((v,1),(-1,v))`.  The separation control has

```text
|p_+|^2=|p_-|^2=13/40,
|D-2*p_+|^2=|D-2*p_-|^2=13/10,
T=5/4, R=13/8, Delta=1/16.
```

The proof ledger records the admitted form and the exact `8 -> 7` / `7 -> 6`
counts required by D-0178.  No research command was run.

## Open

The tangent stratum is reduced, not eliminated.  The transverse roots remain
live under the cheap filters.  Session 157 must combine their diagonal-side
label with a complete strand or nonintersection constraint.

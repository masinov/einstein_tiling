# Session 128 — unequal-spoke topology

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-25`, session 1 of 3

**Mode:** on-paper boundary topology; no experiment

## Proposition

Relax K10B's equal guard legs while retaining its right-angle K9A/K9T clean-
spoke semantics. Determine the minimal boundary change before coordinates.

## Result

N34 proves the old 15-edge word itself forces `u=v`: its one complete side
between `A,B` is simultaneously outgoing spoke `u_A` and incoming spoke
`v_B`; the `B,C` connector repeats the identity.

K16B separates each dual-role connector into the ordered two-side path
`u,v`. Central pairing gives the 17-edge spine

```text
A,u,v,B,u,v,C,u,H,u,C,v,u,B,v,u,A
```

and adding the unequal perpendicular guard legs gives the 19-edge cyclic
word (3.3). The half-turn lens is the exact rectangle `[0,v] x [0,u]`.
Two new bridge angles remain explicit variables rather than hidden markings.

## Decision

Session 129 must derive the complete turn, closure and rectangle system for
this one minimal topology. Adding more sides, participants or a changed guard
angle is outside HC-25.

## Verification

- Each old dual-role connector gives the literal equality `u=v`.
- Splitting its first-half copies and their central partners adds exactly
  four sides, changing 15 boundary sides to 19.
- The centrally paired guard paths form a `v`-by-`u` rectangle.
- No coordinates, angle choices, experiment or artifact were generated.
- Artifact roots remain byte-identical to the HC-25 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.
- Repository tests: `222 passed, 17 deselected` in 145.29 seconds.

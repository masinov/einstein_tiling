# Session 113 — fixed nonconvex shield skeleton

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-19`, session 2 of 3

**Mode:** on-paper boundary/isometry reduction; no experiment

## Proposition

Fix one finite nonconvex boundary word that realizes every intrinsic role,
all K9A primary angles and an exact guard--shield relative isometry before
attempting coordinates.

## Prior art and non-redundancy

N25 forces nonconvexity but does not provide a boundary. K10B is not proposed
as a general jigsaw compiler: it is one named finite skeleton for K9T, under
the already audited matching-rule boundary.

## Result

The 15-edge word

```text
d,A,d,B,d,C,d,H,d,C,d,B,d,A,d
```

uses lengths `A=1,B=2,C=4,H=7`. The two outer `d` sides meet at a right-angle
guard tip; the middle thirteen sides are a centrally paired shield spine.
The first half gives every used code endpoint a complete `d` spoke and the
required `pi/4` angle. The unique host and distinct code lengths recover the
four interval roles; the guard and shield roots are enumerated separately.

K10B proves that a simple spine inside the right-angle lens docks with its
half-turn exactly along the complete spine and has disjoint interior. This
fixes the guard/shield isometry to `x -> -x` and turns the remaining work into
the six explicit coordinate/patch obligations in theory note 25 §8.

## Outcome decision and stop rule

The skeleton survives symbolically and metrically conditionally. Session 114
must provide the actual coordinate list and both complete `ABC/ACB` patches.
If any simplicity, lens, recognition or pairwise-disjointness obligation is
unclosed, HC-19 fires its stop. No alternative word, numerical search or SVG
is admitted.

## Verification

- The length word contains a unique `H=7` and paired `A=1,B=2,C=4` roles.
- Central pairing makes the shield half-turn exact.
- The result is explicitly conditional; no coordinate or existence claim is
  entered in the ledger.

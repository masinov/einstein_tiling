# Session 96 — HC-13 closed-corridor selector and stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-13`, session 3 of 3

**Mode:** on-paper inverse-dissection design; no experiment

## Negative result

N16 proves that the session-95 four-socket scaffold cannot work while its
sockets remain independent. A product corner relation is empty or admits a
constant vertex color and hence a doubly periodic macro tiling. The selector
must be a load-bearing exact-cover component.

## Surviving topology

K5C replaces four sockets by one two-cell-wide closed corridor. Every one of
the 11 source Wang tiles is encoded by a rooted length-42 exact domino word:
a unique six-bar delimiter followed by eight fixed-width bit blocks encoding
its four colors. All words use 42 congruent copies. Four windows at fixed
offsets from the delimiter expose the interfaces.

The finite prefix trie of those 11 words is an explicit selector automaton.
If its transition states are independently visible docking modes and the
geometry forces disjoint bounded cycles, the corridor simultaneously supplies
unique grouping, state selection and four-interface coupling. K5C.1--K5C.6
state the complete admission contract, and K4W then proves aperiodicity.

## Honest boundary

This is an exact combinatorial macro topology, not a polygon. In particular,
the words and trie do not make their states geometric; assigning a state after
reading a word would be circular. No boundary mechanism yet proves finite
cycles, visible transition modes, contact completeness or a lift.

## Checkpoint disposition

HC-13's bounded target is met at the topology level: at least 11 equal-count
rooted retilings and a local, origin-free grouping invariant now coexist in
one coupled design. The cheap variants have exact no-gos. HC-13 is exhausted
and stops with no runner, enumeration, SVG, polygon candidate or large
artifact. Artifact roots at stop are `docs/notebook/assets = 6,756,125,300`
bytes (unchanged) and `data = 1,795,586,222` bytes; the 586,019-byte increase
over the checkpoint baseline is solely the 535,019-byte verified Hu--Lin PDF,
its extracted text and the literature lock update.

A future checkpoint must begin with prior-art review of single-prototile
simulation and geometric synchronizing frames. It may proceed only with one
concrete boundary mechanism for K5C.1--K5C.3 and a predeclared failure outcome.

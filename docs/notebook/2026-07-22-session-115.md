# Session 115 — auxiliary polarity and HC-20 admission

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-20`, session 1 of 3

**Mode:** on-paper contact theorem; no experiment

## Proposition

Correct the remaining stale K10B role sentence and decide what N26 already
controls before asking whether auxiliary same-length sides are globally
uncontrollable.

## Precondition applied

Theory note 25 §5 now says that paired `A,B,C` sides belong to one length
class but have distinct intended-convex and auxiliary-reflex angle contexts.
It no longer repeats the role identity retracted by ERR-008.

## Result

N27 proves that a reflex auxiliary endpoint cannot occur at an internal
straight-host subdivision: the host contributes `pi` and the auxiliary angle
is greater than `pi`, so disjoint interiors would require more than `2pi`.
Thus length plus endpoint polarity recovers the intended K9A role locally.

This does not control contacts away from the primary host. In particular, it
does not show that one matching auxiliary side propagates through the whole
shield arc. That is the exact theorem question reserved for session 116.

## Decision and stop rule

HC-20 remains on paper. Session 116 must give a finite intrinsic
synchronization condition rather than assume full-arc continuation. Session
117 then tests K10B. Coordinates may resume only under K10W's already fixed
reopening rule; no alternative spine, enumeration, fitting or SVG is admitted.

## Verification

- The sector inequality is exact: `pi+(2*pi-alpha)>2*pi` for `0<alpha<pi`.
- N27 explicitly disclaims global contact completeness.
- The checkpoint baseline remains the HC-19 artifact byte counts.

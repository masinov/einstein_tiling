# Session 149 — ERR-013 directed-pairing correction

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-33`, session 2 of 3

## Blocking audit finding

Independent review found that session 148 reversed the traversal direction
of the centrally paired C' edge.  Under the exact pairing
`p_(17-k)=D-p_k`, direct subtraction gives

```text
p_12-p_11=p_6-p_5,
p_15-p_14=p_3-p_2.
```

The user-supplied contradiction was treated as a halt condition.  No further
HC-33 theorem or formula work proceeded before the correction was propagated.

## Corrected result

The arithmetic reset budget and H-west branch of N42 remain correct.  They
prove that H cannot point west, hence

```text
v-2*p_(8,x)>0.
```

The H-east branch is invalid: `p_9` to `p_11` reaches the terminal endpoint
of westward C', not the initial endpoint of an eastward segment.  Corrected
N42 therefore fixes the traversal pattern to `E,W,E,W,E` but eliminates none
of the sixteen K32S/K32A cells.

## Verification and disposition

ERR-013, D-0170, theory note 41, the proof ledger, STATUS and session 148 are
corrected together.  A symbolic regression test now pins both centrally
paired edge-vector identities, while the existing exact arithmetic test is
retained because that portion of N42 survives.

The complete suite passes: `228 passed, 17 deselected` in 141.70 seconds.
Governed artifact roots remain byte-identical to the HC-33 baseline:

```text
docs/notebook/assets  6757298033 bytes
data                  1809060890 bytes
```

K16W is open/frozen.  HC-33 has one session remaining under D-0168; no formula,
solver, artifact, polygon, SVG, patch, decoder, aperiodicity result or
candidate is authorized by this correction.

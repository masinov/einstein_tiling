# Session 130 — unequal-spoke route frozen at exact geometry

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-25`, session 3 of 3

**Mode:** on-paper exact construction audit; no experiment

## Proposition

Give one exact simple K17S spine with fixed bridge angles or prove the minimal
split-spoke topology incompatible.

## Result

Neither terminal result was obtained. A straight bridge is reducible and
leaves K16B. Giving both bridges the existing `pi/4` angle yields the exact
eight-direction identity

```text
w_8=(a-u)+(u-v)q+(v-c)q^2+(b-u)q^3,
```

but does not close the sixteen strict rectangle inequalities and central
host equation. Opposing the two width chords separates neither the fixed
`B,u`/`C,u` hooks nor host closure.

## Decision

HC-25's predeclared stop fires. K16B/K17S remain a precise open finite
geometry problem, recorded as K16W. No numerical angle trial, additional
edge, coordinate fit or candidate follows. Reopening requires a complete
exact witness or a bridge-elimination theorem before computation.

## Verification

- `z_j=1` is exactly the reducible straight-bridge case.
- For `z_1=z_2=q`, paired directions differ by `q^4=-1`, giving (10.2).
- The unresolved predicates are explicitly the K17S inequalities, host
  closure and finite nonintersection table.
- No experiment or generated artifact was produced.
- Artifact roots remain byte-identical to the HC-25 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.
- Repository tests: `222 passed, 17 deselected` in 144.60 seconds.

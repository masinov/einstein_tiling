# Session 93 — HC-12 geometric boundary and K4W compiler

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-12`, session 3 of 3

**Mode:** on-paper consequence of the bounded literature audit; no experiment

## Result

The K3B correction is complete. A local physical contact language with hidden
docking states projects to a binary sofic shift, not necessarily to the
`2x2` SFT of its visible diagonal blocks. Hu--Lin therefore closes only the
bit-complete route. The hidden state must be independently visible in bounded
unmarked geometry, or it is circular.

In the ordinary edge-Wang case, Jeandel--Rao give a design floor: at least 11
macro states and four interface colors. With two diagonal retilings and at
most `h` docking modes per diagonal, `h>=6`.

K4W packages the surviving direct recipe. If every tiling by one polygon
uniquely groups into square macrocells whose exact retilings/docking states
realize a fixed aperiodic Wang set, with complete full-isometry interfaces and
one whole-plane lift, the polygon is an aperiodic monotile. The preferred
inverse-design target uses the minimal 11-state Jeandel--Rao source and seeks
11 rooted exact retilings of one macrocell, with four boundary colors.

## Verification

- N11 is applied only to the bit-complete corner SFT;
- the image of a finite-state SFT under a site map is described only as sofic;
- N13 is scoped to ordinary edge-Wang macro languages;
- K4W separately proves tileability, unique all-tilings decoding, period
  descent and full-isometry coverage;
- no source serialization, polygon, contact atlas or novelty is claimed;
- complete fast suite: `222 passed, 17 deselected`;
- artifact roots at stop: `docs/notebook/assets = 6,756,125,300` bytes
  (unchanged) and `data = 1,795,000,203` bytes (1,350,246 bytes above the
  HC-12 baseline, solely the ignored Kari--Moutot and Jeandel--Rao literature
  cache and lock update);
- no runner, enumeration or generated geometric artifact was used.

## Stop

HC-12 is exhausted at three sessions. The bit-only boundary-synthesis route
is closed. The surviving K4W inverse-dissection route is frozen pending a new
human checkpoint. If reopened, it receives a three-session on-paper kill
condition: exhibit one exact macrocell topology capable of at least 11 rooted
retilings plus a plausible unique-grouping invariant, or stop without a shape
run.

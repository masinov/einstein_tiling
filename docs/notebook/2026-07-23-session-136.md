# Session 136 — non-right spoke interval

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-28`, session 2 of 3

## Question

Before attempting closure or simplicity, determine exactly which non-right
guard angles can even place all three full-length `d` spokes with endpoints in
the rhombic lens. This is an analytic family question, not an angle sweep.

## Result

K23I proves, up to reflection, that the only surviving non-right range is

```text
pi/3 < gamma < pi/2.
```

It also fixes the remaining orientation freedom as
`delta=(pi-gamma)/2-epsilon` with
`0<epsilon<min(3*gamma-pi,(pi-gamma)/2)`. The proof uses exact projective
oblique coordinates of a unit chord; no sampled angles or coordinates enter.

K23C then checks that the surviving containment system is genuinely nonempty.
With rational half-angle, rational global direction and rational weights, all
six oblique partial sums lie strictly in the unit square. The same tuple has
`h=23/10>8/5`, the exact rhombus diameter, so it is not a closure witness.

## Verification

- the isolated unit chord criterion is exactly `|r|,|s|<1` in rhombic
  coordinates;
- the three spoke lines differ by `gamma` modulo `pi`;
- interval membership gives the strict lower threshold `3*gamma>pi`;
- every fraction in theory note (9.4) is strictly between zero and one;
- `2*cos(gamma/2)=8/5<23/10`.

These checks are finite exact arithmetic used to verify a written derivation,
not a research experiment.

## Decision

HC-28 remains active at session 2 of 3. The final obligation is the coupled
containment-to-diameter implication on `pi/3<gamma<pi/2`, or a complete exact
simple witness. Failure of both fires the checkpoint freeze without a solver
run or parameter search.

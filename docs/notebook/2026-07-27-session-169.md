# Session 169 — HC-40 full-side hinge compiler

**Date:** 2026-07-27

**Checkpoint:** `HC-2026-07-27-40`, session 3 of 3

## Objective

Audit the cheapest full-side realization of both K46S states. Derive the
necessary port equalities, exclude mixed states by theorem, and emit an exact
rooted boundary template rather than an unconstrained polygon search.

## Result

Theory note 55 proves:

- K47P: both states are realizable at boundary-germ level exactly when each
  root side `H,D` has one `alpha` and one `beta` endpoint and the four non-root
  incident sides cross-match into two lengths `X,Y`;
- K47T: within the reflection-invariant four-star, the two mixed assignments
  would require `alpha=beta=pi/2`, so `alpha!=beta` makes the selected states
  the complete local sector language;
- K47B: every full-side carrier belongs to the rooted cyclic template
  `H,Y,P,Y,D,X,Q,X`, with specified endpoint angles and two named residual
  arcs. With nonempty one-side-minimum residual arcs it has at least eight
  sides.

This is an exact candidate-generation filter, not a polygon. The residual
arcs still carry global closure, simplicity, termination and spurious-contact
burdens.

## Verification

The two selected states use respectively the equal pairs
`X=(alpha at H)=(beta at D)` and
`Y=(beta at H)=(alpha at D)`. The remaining two sector assignments have
angle sums `4*alpha` and `4*beta`; either can equal `2*pi` only at the excluded
case `alpha=beta=pi/2`.

The complete suite passes:

```text
239 passed, 17 deselected
```

## Decision

HC-40 succeeds at its predeclared finite-admission outcome and is exhausted.
The literal three-participant hinge is refuted, while its forced four-star has
exactly two rooted states and one finite full-side boundary template. No
coordinate, experiment, SVG, polygon or candidate is claimed.

The natural next theorem checkpoint is the smallest-template question:
derive the global turn/closure equations for one-side residual arcs `P,Q` and
either construct an exact simple eight-side support or prove that one of the
residual arcs must be longer. That requires new human authorization.

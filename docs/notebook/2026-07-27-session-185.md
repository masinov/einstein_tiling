# Session 185 — full SAB-germ test of the 17-rhombus compiler

## Experiment pre-registration

### Proposition

Reconstruct the ordered endpoint tangent germs of every published Section
10.1 SAB component from the pinned vector source, calibrate the matching
predicate against all internal contacts of the two published 15-rhombus
macros, and decide whether any reflected-state choices for the four added
singleton `M` cells make the exact 17-rhombus relation
`large_A + 2M <-> large_B + 2M` legal with the same complete outer SAB-germ
signature.

This is the missing distinction between the session-181 collapsed-endpoint
test and the paper's full rule that SABs continue across patch-tile edges.

### Prior art and non-redundancy

Akiyama--Hamada--Ito, Section 8.1 and Remark 8, require edge-to-edge tilings
whose SABs continue across patch-tile boundaries; the isometric cells bend
the SAB rather than extending the cabinet-cell segment.  The pinned v3
source supplies the vector paths but no endpoint-germ or contact table.
Session 175 recovered only axis and ordered corridor bits, while session 181
checked only coincidence of collapsed SAB endpoints.  No source or repository
artifact currently decides the full-germ legality of this 17-rhombus
regrouping.

### Outcome decisions

- If the calibrated full-germ rule validates both published macros and at
  least one singleton-state assignment validates both 17-rhombus
  decompositions with equal outer signatures, retain the fixed polygon as a
  genuine finite source compiler kernel.  The next obligation is still a
  whole-plane tiling and total grouping/decoder; no monotile is claimed.
- If the published contacts calibrate but no assignment works, retract the
  session-181 compiler interpretation and retain the 17-rhombus object only
  as a collapsed-endpoint false positive.  The failing endpoint germs become
  a family-level obstruction to this exact equalizer.
- If the source vectors do not determine a unique contact predicate, record
  the ambiguity and stop; do not choose a visually convenient rule.

### Stop rule and finite justification

Use only the 31 pinned source paths, their fixed atlas embeddings, the 44
published internal macro contacts, the four already-certified equalizers,
and the `2^2` reflected singleton choices on each side.  Do not alter the
support, add padding, enlarge a contact radius, search polygon coordinates,
or test another family.  This finite extensional table completely decides
the full-germ status of the fixed relation.

### Human checkpoint

`HC-2026-07-27-46`, session distance two.  The user's direct
compile-or-impossibility instruction controls the research direction;
checkpoint accounting remains background governance.

## Results

Both published macros have unique endpoint-germ lifts and 22 internal
contacts.  Collectively those legal contacts realize every odd direction
difference `{1,3,5}` modulo six.  Hence exact straight tangency is not a
source boundary color; the stated rule is endpoint continuation.

All four equalizers have four legal singleton-state assignments on each
side under that rule and identical exposed endpoint signatures.  Exact
tangent signatures never agree, which is retained as a diagnostic refuting
the stronger invented rule, not as a source-language failure.  Two
orientation-preserving equalizers also agree under the stricter inferred
odd-difference compatibility relation; the reflected two do not.

## Verification

The producer and cold verifier independently re-extract the 31 vector paths
from the pinned source archive and reproduce the finite tables.  Artifact:

```text
data/sturmian-source/ahi-seventeen-rhombus-full-germs.json
sha256 831ec3d67f415462ddebd75288013d2fff8bc6dd1fa645a5656322abeb5dfcdd
```

## Decision

ERR-017 withdraws K54S.  `P17` is a genuine local compiler, but N60C proves
that its fixed `large+2M` state cannot realize the whole source frequency.
The only area-compatible alternative is seventeen singleton cells; N60V
then excludes it at the three-axis hexagon vertex because three binary
corridor bits cannot be pairwise unequal.  N60P therefore closes every
carrier-local P17 decoder.  Cross-boundary decoders are outside the theorem.

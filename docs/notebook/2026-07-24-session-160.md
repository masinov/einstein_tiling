# Session 160 — HC-37 second bridge and selector table

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-37`, session 2 of 3

## Admission

This remains theorem-only work under D-0182.  It consumes the second bridge
chart and the complete K32S signs.  No solver, CAS, numerical sample,
coordinate fit or experiment is admitted.

The desired result is a uniform negative K36P selector on a complete cell.
Eliminating algebraically impossible selector attacks may be retained, but it
does not satisfy HC-37 unless a live root/cell/variable/degree count falls.

## Result

K39C rotates the C initial point into the B frame and obtains
`m_C=dot(z_2,U)`.  On either fixed second-bridge semicircle, the two vertical
endpoints give opposite offset signs whenever `U_y` is nonzero.  Thus
`sigma_2` alone cannot force the C selector.

K39O then uses the complete strand signs.  Root monotonicity makes six of the
twelve root-labelled B/C selector attacks strictly positive.  The only
remaining possible negative attacks are

```text
S1+: B,C;  S2-: B,C;  S3-: B;  S3+: C.
```

## Verified

The C identity follows from `rho_C/rho_B=-i*z_2` and
`Im(i*conj(z_2)*U)=Re(conj(z_2)*U)`.  Every row of the attack table was checked
directly against `L_B^+<L_B^-` and `L_C^+>L_C^-`.  No research command was
run.

## Result boundary

The `12 -> 6` attack reduction is not an admitted HC-37 success: all six K34Q
cells and both roots remain open.  No selector is proved negative.  Session
161 is the checkpoint's final attempt; failure to lower an admitted count
freezes the thin-lens carrier.

# Session 104 — HC-16 common-collar reduction

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-16`, session 2 of at most 3

**Mode:** on-paper exact geometry; no experiment

## Proposition

Determine the positive-length boundary condition immediately below K7A's
four legal right-angle T-junctions, without introducing an extra participant
or an unlisted partial-contact repair.

## Prior art and non-redundancy

The edge-patch framework remains prior art. This session only derives the
length equalities forced when each neighbor--neighbor stem is a complete
clean side contact in both selected words.

## Result

K7C proves that all five used leaving sides have one common length `d`. The
two admitted words therefore consist of three disjoint rectangular collars
through the entire open strip `-d<y<0`, independently of their order.

Removing those collars leaves three rooted tails. Their exact offsets for
`ABC` and `ACB` are now written explicitly in theory note 22, so every
remaining disjointness obligation is a finite set intersection below the
collar rather than an informal picture.

This is a reduction only. No tail, polygonal disk or whole-plane tiling is
claimed.

## Decision and stop rule

Session 105 is the final HC-16 session. It may supply exact polygon
coordinates and hand-check the two tail packings, or it must freeze the
selected-word route. A stem mismatch may not be repaired by adding another
junction, contextual handedness or participant, because those were excluded
at checkpoint admission. No search, optimizer or generated drawing is
authorized.

## Verification

- The proof is a direct equality chase over the four selected junctions and
  a rectangular-coordinate decomposition of the clean collar.
- `git diff --check` and the governance test are run before commit.
- No research runner or generated research artifact was created.

# Session 139 — HC-30 rigid-hook crossing cone

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-30`, session 1 of 3

## Authorization and proposition

Apply K25X's exact intersection method to K16B before any new family or
decision run. The first bounded pair is intrinsic to each bridge-independent
`v,w,u` hook, `w in {b,c}`: its first and third segments are nonadjacent and
have a fixed relative quarter-turn.

## Outcome rules

- If the two hooks and established K17S containment close K16W, record the
  family no-go and stop.
- If they do not, derive the strongest exact common parameter cone they force
  and carry that cone, rather than the old unrestricted sentence, into the
  bridge-dependent pair analysis.
- A local formula without a strict global reduction does not count as a
  terminal HC-30 outcome.

## Method and artifact rule

Theorem only. No runner, solver, angle sample, coordinate fit or render is
admitted. The HC-30 artifact baseline is
`docs/notebook/assets=6756513353`, `data=1809060890`.

## Results

1. **K26X.** In either rigid hook, the first and third segment parameters
   are

   ```text
   t=1-w/(sqrt(2)*v),   s=w/(sqrt(2)*u).
   ```

   Thus simplicity requires `w>sqrt(2)*min(u,v)`; equality at this smaller
   threshold is a forbidden nonadjacent endpoint contact.
2. **K26P.** K19P sharpens to `v>u/sqrt(2)`: the first two contained
   vertices give `v>a*x>u*(x+y)/sqrt(2)>u/sqrt(2)`.
3. **N38.** Applying K26X to both `b` and `c`, using
   `a>u/sqrt(2)` and the strict host-chord bound
   `a+b+c<sqrt(u^2+v^2)`, eliminates `v<u` and forces

   ```text
   v/u > sqrt(23/2).
   ```

4. In this surviving wide cell, each long `v` spoke in the two hooks has
   direction `(x,y)` satisfying

   ```text
   x*y>0,   y^2<2/23,   x^2>21/23.
   ```

   Hence the two symbolic bridge phases lie in four explicit horizontal
   polarity cells. This is a strict global reduction but not yet HC-30's
   terminal crossing-pattern decomposition.

## Exact checks

- The hook formula is obtained by dividing
  `t*v=v+w*q+s*u*q^2`, with
  `q=(-1+i)/sqrt(2)` and `q^2=-i`, into real and imaginary parts.
- If `v>=u`, the strict chord bounds give
  `sqrt(u^2+v^2)>5u/sqrt(2)`, hence `v^2>23u^2/2`.
- If `v<u`, K26P gives `v>u/sqrt(2)`, while K26X makes
  `a+b+c>u/sqrt(2)+2sqrt(2)v>sqrt(u^2+v^2)`, a contradiction.
- In the wide cell, containment of the middle code edge gives
  `|x-y|<1`; a unit vector with `x*y<=0` has `|x-y|>=1`.

## Decision

K16W is not yet closed. Session 140 must use the two bridge phases
symbolically to partition the remaining long-spoke intersection patterns;
it may not revert to the monolithic K21Q sentence.

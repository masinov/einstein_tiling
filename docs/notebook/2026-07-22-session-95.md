# Session 95 — HC-13 interface-arity no-gos

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-13`, session 2 of at most 3

**Mode:** on-paper topology analysis; no experiment

## Results

N14 closes the raw K5S ownership channel. Its east/west signatures are fixed
and its north/south signatures coincide, so any nonempty ordinary Wang
interpretation admits a constant doubly periodic macro tiling.

N15 closes the obvious `4 x 4` repair. Four independently flippable `2 x 2`
domino quadrants produce 16 exact states and side signatures, but the four
bits are exactly binary macro-corner colors. Any selected language is the
Hu--Lin `2 x 2` corner SFT already refuted by N11.

K5Q retains a non-binary corner-source route. A conditional period-descent
theorem replaces four Wang edge colors by four shared corner modes. A
`20 x 20` domino scaffold can hold four disjoint `2 x 5` sockets, each with
eight exact retilings, while a fixed central rail supplies the K5S six-bar
synchronizer. This is an exact high-capacity topology, not an enforced
language: the missing central selector must admit exactly the desired corner
tuples, and one unmarked polygon must force the scaffold in every tiling.

## Source boundary

Hu--Lin's introduction points to a published 44-tile aperiodic corner system
on six colors. That statement is recorded only as an unaudited lead. It is
not used as a theorem or as authorization for a search.

## Stop condition for session 96

The final HC-13 session must decide whether the socket relation and
synchronizer can be coupled by a non-circular exact topology. If not, the
synchronized-domino parameterization closes without geometry or computation;
K4W/K5Q remain conditional specifications only.

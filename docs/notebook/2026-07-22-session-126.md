# Session 126 — complete K10B lens system

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-24`, session 1 of 3

**Mode:** on-paper exact geometry and arithmetic; no experiment

## Proposition

Derive the complete K10B spine and every square-lens containment condition
before selecting a replacement weight tuple.

## Result

K15S gives all thirteen spine edges from six explicit partial sums in the
`Q(sqrt(2))`-linear span of `(a,b,c,d)`. Because K10B never fixed the
terminal `A` angle, a unit direction `(C,S)` remains. Complete open-lens
containment is exactly 12 two-sided coordinate bounds (24 scalar strict
linear inequalities) conditional on that
direction, while host closure is one line meeting the unit circle.

N32 derives the additional necessary wedge

```text
b+c<a<(1+sqrt(2))*b-c,       b>sqrt(2)*c.
```

This refutes the illustrative `(11,4,6,21)` tuple, despite its passing N31.
K15D replaces the usually unavailable `d>h` shortcut: since `2d>h`, an
unintended full `d` host cover exists arithmetically exactly when
`h-d` belongs to `<a,b,c>`.

## Decision

Session 127 must solve the complete exact system jointly with U1/U2 and
K15D without a numerical search. No tuple, coordinates or candidate is
promoted in this session.

## Verification

- The six partial sums follow from successive `3*pi/4` rotations.
- Central pairing reconstructs all remaining vertices and the host edge.
- Expanding the host-length equation gives the line (4.2) exactly.
- N32 treats both signs of `sqrt(2)d-b` and uses strict open-lens bounds.
- No experiment or generated artifact was produced.
- Artifact roots remain byte-identical to the HC-24 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.
- Repository tests: `222 passed, 17 deselected` in 145.03 seconds.

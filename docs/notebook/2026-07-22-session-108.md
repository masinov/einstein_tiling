# Session 108 — one-edge cap continuation and HC-17 stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-17`, session 3 of 3

**Mode:** on-paper local topology; no experiment

## Proposition

Determine whether the K8U exposed segment can terminate in one cap side,
using the K7A reflex endpoint at its lower corner, without another occurrence
or an unspecified continuation.

## Prior art and non-redundancy

This is the exact finite endpoint left by N22/K8U. It is a local polygonal
disk question and makes no novelty claim for matching rules, T-junctions or
symbolic compilers.

## Result

The tempting endpoint pairs the cap's `3*pi/2` reflex angle with a convex
`pi/2` angle of the longer neighbor. Although the sectors fill `2*pi`, N23
proves that a two-tile common boundary cannot end there: as the local
separator between two disk interiors it must have an outgoing branch. The
cap and longer neighbor therefore share another side.

If the new side lengths differ, N22 creates another participant. If they
agree, the same endpoint obligation moves to the next vertex. No finite
multi-edge interface returning to the original shorter occurrence was
derived.

## Decision

The HC-17 stop fires. N22, K8U and N23 are retained as exact filters; no
polygon, coordinate search, SVG, generated artifact or candidate follows.
This does not refute a future explicitly supplied finite multi-edge cap
cycle. Reopening requires its complete cyclic boundary word and placement
isometries before computation.

## Verification

- Full fast suite: `222 passed, 17 deselected` in `145.53 s`.
- `git diff --check`: clean.
- Checkpoint roots are byte-identical to the HC-17 baseline:
  `docs/notebook/assets = 6,756,125,300` bytes and
  `data = 1,809,060,890` bytes. No research artifact was generated.

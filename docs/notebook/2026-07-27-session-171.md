# Session 171 — HC-41 exact simple octagon support

**Date:** 2026-07-27

**Checkpoint:** `HC-2026-07-27-41`, session 2 of 3

## Objective

Solve K48C exactly and prove or refute simplicity of the resulting support
without numerical search. Stop immediately if a role-safe exact witness meets
the checkpoint's quantified success condition.

## Result

Theory note 57 gives the exact data

```text
alpha=pi/3, beta=2*pi/3,
(r_1,r_2,s_1,s_2)=(pi/3,-pi/3,-pi/3,pi/3),
(h,y,p,y,d,x,q,x)=(13,3,2,3,6,4,5,4).
```

The resulting vertices are

```text
(0,0), (13,0), (29/2,3*sqrt(3)/2),
(27/2,5*sqrt(3)/2), (15,4*sqrt(3)),
(9,4*sqrt(3)), (7,2*sqrt(3)), (2,2*sqrt(3)).
```

K49W proves exact side lengths, root/residual angles, closure, pairwise role
distinction and simplicity. Every nonadjacent edge pair has disjoint exact
axis-aligned bounding boxes; no crossing test is numerical.

## Decision

HC-41 meets its exact-simple-support outcome after two sessions and closes
early. The data are an eight-sided **carrier support witness at boundary-germ
level**, not a tile candidate. K46J's complete four-copy placements, remote
contact termination and full local atlas are still open, so no SVG, search or
candidate promotion is authorized.

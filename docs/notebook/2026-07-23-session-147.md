# Session 147 — HC-32 finite strand and direction atlas

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-32`, session 3 of 3

## Target

Attempt an exhaustive strand-order theorem on the K31C-bounded survivor and
state a sound later-decision contract.  Selected orderings, generic-line
assumptions and dropped pair predicates are forbidden.

## Results

1. Every one of the four length-`v` segments and the central host crosses
   the fixed symmetry line `x=v/2`.  Their heights are
   `y_B,y_C,1/2,1-y_C,1-y_B` and are pairwise distinct.
2. The exact hook offset `p_5-p_2=(A+iB)r_b`, with `A,B>0`, proves that the C
   strand lies above the B strand throughout their common interval.
3. **K32S (exact exhaustive partition).** Central symmetry and that strict
   order leave exactly four midline strand cells, listed in theory note 40.
   No cell is asserted nonempty.
4. **K32A (exact finite cover).** Two bounded tangent charts cover each
   bridge circle minus the two forbidden straight directions.  Four chart
   pairs cover both bridges without adding a continuous variable or raising
   degree.
5. **K32R (exact exhaustive partition).** The closure discriminant separates
   impossible, tangent and transverse terminal-orientation strata.  Interval
   Newton is relevant only on the transverse stratum; no complete interval
   decision follows for free.

The remaining K16W obligation is therefore covered by `4 x 4=16` bounded
cells.  Each retains every original containment, closure and nonintersection
predicate.

## Verification and disposition

All ordering and chart cases are derived exactly; no enumeration program,
solver, coordinate fit or sampled angle was used.  Governed artifact roots
remain byte-identical to the HC-32 baseline.  HC-32 is exhausted at three
theorem sessions.  Any HC-33 decision requires new explicit authorization,
pre-registration, all 16 cells fixed in advance, and an exact treatment of
SAT witnesses, UNSAT certificates and tangent/strict-boundary strata.

The complete fast regression suite passes: `226 passed, 17 deselected` in
141.90 seconds.

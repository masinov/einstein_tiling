# Session 144 — HC-31 decomposed K16W result

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-31`, session 3 of 3

## Proposition and fixed scope

Run exactly the two complete cells fixed in session 143, in order `P_+-`,
`P_-+`.  Each formula retains all 120 nonadjacent-segment predicates and adds
N38, its four polarity signs and K29O.  The fixed external wall limit is four
hours per cell; no fallback, altered ordering or extra budget is authorized.

## Result

Both externally supervised processes reached the fixed wall limit:

| cell | status | external return | elapsed seconds | formula SHA-256 |
|---|---|---:|---:|---|
| `P_+-` | `resource_stop` | 124 | 14400.050474520001 | `da7bf13cd6297ce5a3c2340a39ec7ad3c4e70b8228c78648241d5b736717afff` |
| `P_-+` | `resource_stop` | 124 | 14400.062070380998 | `1491aa2cb14b41873090e9cef12e34b6c7f906d558deec248dd64ce0d8019c60` |

Neither cell returned SAT, UNSAT or a model.  No cold verification was
possible or needed.  This is not evidence that either cell is empty or
nonempty; K16W remains open/frozen.

## Verification

- The aggregate manifest records code version `d30342f`, the fixed cell
  order and `complete=true`.
- Recomputed SHA-256 values match both result records.
- Each preserved SMT-LIB formula contains exactly 174 top-level assertions,
  matching `13+32+1+120+8`.
- The screen ended normally after the second supervised stop; it was not
  interrupted or relaunched.
- Governed artifact roots after the run are
  `docs/notebook/assets=6757298033` and `data=1809060890`, growth 784,680
  bytes from the HC-31 baseline and far below the 1-GiB stop.
- The complete fast regression suite passes: `226 passed, 17 deselected` in
  144.61 seconds.

## Decision

HC-31 is exhausted at three sessions.  The solver result is recorded as a
resource stop, not promoted to a mathematical verdict.  Any future exact
decision must be based on new proved necessary conditions or exact
equivalences; repeating these two formulas with another timeout is forbidden.

The user explicitly authorizes HC-32 after reviewing a five-part conservative
reduction plan.  HC-32 is theorem-only: exact budget and weight bounds,
orientation/mirror equivalences, compactification at `v=infinity`, and an
exhaustive strand-order question.  Every reduction must be ledgered as either
an implication from the original K16W system or an exact equivalence.  No
replacement solver run is authorized.

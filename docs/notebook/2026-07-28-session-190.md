# Session 190 — complete sub-30 carrier-local classification

## Experiment pre-registration

### Proposition

Exhaust the complete geometric **superset** of carrier-local Sturmian source
states of area below 30 identified by K64A.  Normalize each published
`large_A` and `large_B` support, attach exactly `m=0,1,2` congruent common
rhombi with disjoint interiors so that the union is a connected topological
disk, and deduplicate equal supports.  For every resulting area
`A=15+m` support, enumerate every subdivision of its primitive triangles into
`A` common rhombi.  The run will decide whether any all-singleton subdivision
has a bipartite long-diagonal continuation graph, the necessary K64B
condition for the `(0,A)` carrier state.

The attachment list deliberately includes geometrically possible states that
may fail the stricter AHI endpoint-germ or vertex rules.  Therefore a zero
survivor result proves that no carrier-local Section 10.1 compiler of area
below 30 exists.  A survivor proves only that this necessary parity gate does
not close its exact support; it is not a tile or compiler until its boundary
germs, source vertex stars, whole-plane grouping, total decoder, and
periodicity gates are separately certified.

### Prior art and non-redundancy

The primary Akiyama--Hamada--Ito source supplies the exact `15,15,1`
common-rhombus macro areas and the ordered-corridor continuation rule.
Repository K64A proves that the only carrier-local areas below 30 are
`15,16,17`; K64B proves bipartiteness is necessary for the all-singleton
state.  N60V previously enumerated only the 60 subdivisions of one particular
17-rhombus equalizer.  No located source classifies all supports obtained by
adding zero, one, or two common rhombi to either published large macro.  This
run completes that finite theorem-defined class; it is not a radius, area, or
candidate-search escalation.

### Outcome decisions

- If every subdivision is nonbipartite, preserve a shortest exact odd-cycle
  witness per support and promote the exhaustive result to the theorem that no
  carrier-local Sturmian compiler of area below 30 exists.  The carrier-local
  constructive frontier then jumps to area at least 30.
- If one or more bipartite subdivisions survive, serialize every survivor and
  stop.  Those exact supports become the complete sub-30 input to endpoint-
  germ and source-vertex legality; no polygon or monotile claim is authorized.
- If the generator exposes a representation ambiguity, fail closed and record
  it.  Do not narrow the attachment class to make the census finish.

### Stop rule and finite justification

The run covers exactly two fixed published macro supports and attachment
budgets `m in {0,1,2}`.  Attachments are generated from all full-isometry
images of the pinned singleton rhombus sharing at least one unit boundary edge
with the growing union; only interior-disjoint connected disk unions are
retained.  Every final triangle set has at most 34 triangles, so its lozenge
subdivisions are the perfect matchings of one finite adjacency graph.  Stop
after all six `(macro,m)` classes and cold verification.  No larger area,
attachment budget, collar, radius, solver, or timeout is authorized by either
outcome.

### Human checkpoint

Current checkpoint `HC-2026-07-27-47` records through session 187.  This is
session distance three, within the repository's three-session rule, and the
expected finite JSON certificate is far below the remaining 1 GiB artifact
budget.  The user's persistent instruction explicitly authorizes fully
following the source-language/component-decomposition route and prioritizes
distance to the Sturmian monotile theorem over checkpoint ceremony.

## Results

The gate passed before the producer was written.  The single producer run was
launched through `scripts/run_research.py` and completed in 6.1 seconds:

```text
supports=997  perfect matchings=29443  bipartite=0
```

The exact class counts are:

| macro | attached `M` | supports | matchings | bipartite |
|---|---:|---:|---:|---:|
| `large_A` | 0 | 1 | 27 | 0 |
| `large_A` | 1 | 28 | 816 | 0 |
| `large_A` | 2 | 472 | 14,650 | 0 |
| `large_B` | 0 | 1 | 24 | 0 |
| `large_B` | 1 | 28 | 736 | 0 |
| `large_B` | 2 | 467 | 13,190 | 0 |

Every support record includes its exact primitive cells, complete matching
count, and a shortest odd-cycle witness.  The run applied no source-rule
filter, so these 997 supports are a superset of the actual AHI-legal carriers.

## Verification

The cold verifier rebuilt every attachment and every perfect matching:

```text
verified sub-30 carrier classification:
supports=997 matchings=29443 bipartite=0
```

The independent P17 regression anchor is present under both source macro
types and reproduces its previously pinned `60 -> 0` subdivision count.
Targeted tests pass:

```text
5 passed
```

The complete repository suite passes:

```text
281 passed, 17 deselected
```

Artifact pin:

```text
ahi-sub30-carrier-classification.json
sha256 20ae6ff936eb0674ab0c7facf2a9e21969b0cdb71dbbd0287aaa4a6bccc6fc9c
bytes  4779407
```

## Decision

Promote K64C/N64S: no connected common-rhombus carrier whose decoded source
macrotiles remain inside one carrier can compile the exact AHI Section 10.1
system at area below 30.  K64A exhausts the possible areas and the geometric
superset census kills the mandatory all-singleton state at each one.

Do not read the result as a general Sturmian-monotile no-go.  Area 30 and
larger carrier-local systems remain open, as do cross-carrier and genuinely
nonseparable decoders.  The next mathematics should use the minimum jump to
decide whether area 30 admits a finite composition/state classification or
whether cross-carrier decoding is structurally unavoidable; it should not
invent an unrelated boundary gadget.

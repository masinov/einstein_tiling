# Session 197 — geometric erasure of the zipper terminals

## Done

- Replaced K70Z's painted phase-zero endpoints by two directed length-four
  delimiter roles and fixed three-participant cap stars.
- Proved K71T: the exact length-eleven cover
  `D_L,E_1,E_2,E_3,D_R` forces incoming phase zero, phase continuity, and
  outgoing phase zero, hence exactly the four AHI even-parity words.
- Proved delimiter reversal cannot imitate the opposite complete terminal
  word under the full Euclidean group.
- Proved K71B: any finite collection of convex rooted side germs can coexist
  on one connected symmetry-free simple polygon via separated shallow tabs.
- Applied K71B to the host, two delimiter, four code, and cap roles.

## Verified

- Terminal sectors sum exactly:
  `5pi/6+pi/2+2pi/3=2pi`.
- Internal matched phase sectors sum `pi`; mismatches remain
  `11pi/12` or `13pi/12`.
- The length identity is `4+1+1+1+4=11`; after anchored delimiters the
  residual interval has length three and admits exactly three unit roles.
- All prescribed endpoint angles lie strictly between zero and `pi`, so the
  separated-tab embedding lemma applies.

## Failures / boundaries

- K71B places the required side germs on one polygon but does not make copies
  using those sides pack simultaneously around the host.
- The terminal cap is a rooted complete germ; the final polygon must still
  exclude imitation by other `2pi/3` vertices.
- No unintended-contact atlas, source lift, whole-plane tiling, periodicity
  result, or monotile exists.

## Open

Prove a simultaneous congruent-packing lemma for all four parity words, or a
no-go for the K71T role system.  This is K70P's next unresolved clause.

No experiment, enumeration, solver, coordinate search, SVG, or candidate was
opened.

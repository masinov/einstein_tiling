# Session 26 — binary quotient-family certificates

**Date:** 2026-07-17  
**Scope:** retain 0/1 exact-cover information while moving from finite quotient
logs to infinite HNF families.

W1's complete transfer certificates exclude all 126 nonzero period vectors
with `Q(v)≤36`. An HNF `(a,b,d)` contains `v=(x,y)` exactly when `d|y` and
`a|(x-(y/d)b)`. Composing these facts gives a compact binary quotient-family
certificate with an independent arithmetic verifier.

Results:

- every HNF of index at most 36 is covered;
- index 37 first leaves `(37,10,1)` and `(37,26,1)` unresolved;
- 2,941/8,864 area-admissible HNFs through index 215 are excluded;
- exact D6 actions extend the thin obstruction to `(1,0,k)`, `(k,0,1)`, and
  `(k,k-1,1)` for every `k≥4`.

Artifact:
[`theory-w2-binary-families.json`](assets/theory-w2-binary-families.json),
SHA-256 `770f24ffd2a12bed698dc19701e1e75676713849c58cb9f569c37c509f2bf8c7`.

These are genuine infinite binary exact-cover exclusions. HNFs outside the
families remain unknown.

# Session 129 — exact rectangular-lens system

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-25`, session 2 of 3

**Mode:** on-paper exact turn system; no experiment

## Proposition

Derive every K16B partial sum, rectangle inequality and closure equation, and
decide whether its two bridge angles are legitimate geometry or hidden
symbolic colors.

## Result

K17S gives all eight first-half partial sums using the fixed sharp-turn unit
`q=(-1+i)/sqrt(2)` and two bridge-turn units `z_1,z_2`. Central pairing
reconstructs the remaining vertices in the `v`-by-`u` rectangle. Complete
lens closure is sixteen strict coordinate inequalities plus one exact central
length equation; the latter reduces to the line (7.1) in the terminal unit
direction.

K17G proves the bridge angles are intrinsic geometry. Their incident lengths
form the ordered unequal pair `(u,v)`, so a Euclidean isometry preserves and
locally exposes each angle. They may be chosen once as shape parameters, but
cannot vary by occurrence like colors.

## Decision

Session 130 must give one exact simple K17S spine with fixed bridge angles or
prove K16B incompatible. Numerical fitting, angle enumeration and further
topological relaxation are excluded.

## Verification

- The direction exponents in (6.2) reproduce all seven turns in (5.2).
- Equation (6.4) gives the exact central reversal around `(v/2,u/2)`.
- Direct expansion of the host length gives (7.1)--(7.2).
- K17G uses only Euclidean invariance of an ordered unequal side pair and its
  incident angle.
- No coordinates, experiment or artifact were generated.
- Artifact roots remain byte-identical to the HC-25 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.
- Repository tests: `222 passed, 17 deselected` in 145.07 seconds.

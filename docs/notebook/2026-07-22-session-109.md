# Session 109 — HC-18 gates and four-sector selector

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-18`, session 1 of 3

**Mode:** housekeeping plus on-paper angle algebra; no experiment

## Audit preconditions

The five zero-byte files matching failed literature-download temporary names
were removed in one sweep only after confirming a nonempty canonical PDF
beside each. An exhaustive zero-size scan also found three ignored compiled
research streams. They were retained and classified from the binary headers,
generators and original notebooks:

- `data/a1-compiled/periodic-13.jsonl` is an intentional empty certificate
  stream because 13-kite copies require quotient index divisible by 13 and
  the frozen A1 schedule ends at 12. It means zero cases were admitted, not
  that periodicity was refuted.
- `data/a2-compiled/depth2-witnesses-09.jsonl` is an intentional empty
  positive-witness stream. Its survivor and exhausted companions are both
  valid 16-byte `A0PK` headers with count zero, agreeing with the recorded
  exact `69 -> 0` depth-2 result.
- `data/a2-compiled/depth3-witnesses-11.jsonl` is likewise intentional. Its
  companion headers both have count zero, agreeing with the exact
  `11 -> 0` depth-3 result.

No research data file was deleted. The distinction is now recorded in the
experiment registry so an empty stream cannot later be promoted into a
stronger claim.

## Proposition

Determine the exact word-selection algebra when one additional congruent
occurrence contributes a fixed positive sector at each internal host
subdivision point.

## Prior art and non-redundancy

This is the explicitly authorized escape from J0. K7A assumes three
participants and K8U adds a participant only at a later unequal-stem
endpoint. Neither covers a positive fourth sector at the primary point. No
method- or novelty claim is made.

## Result

K9A is exact. If the guard angle is `gamma`, all four adjacencies in
`ABC,ACB` close precisely when

```text
ell_B=ell_C=theta,
rho_A=rho_B=rho_C=pi-gamma-theta.
```

The class with `A` in the middle closes exactly when `ell_A=theta`, so a
different `ell_A` selects exactly the desired two reversal classes under full
isometries. Unlike K7A, the primary sector equations allow every listed angle
to be convex. Complete guard spokes additionally force only two length
classes, not K7C's common stem.

## Outcome decision and stop rule

The angle system survives, so session 110 may test its convex curvature and
interface endpoints. It may not draw a polygon. If the positive guard cannot
be assigned a finite bounded residual contact topology by session 111,
HC-18 freezes without coordinates, enumeration or optimization.

## Verification

- The three binary companions decode as valid `A0PK` version-1 headers with
  `n=9/11`, reserved field zero and record count zero.
- The original sessions 13, 15 and 16 and D-0020/D-0022/D-0023 give the
  matching scheduler and census semantics.
- `find data/literature/papers -type f -size 0` returns no PDF remnants after
  the authorized sweep.

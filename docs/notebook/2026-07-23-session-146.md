# Session 146 — HC-32 exact K16W compactification

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-32`, session 2 of 3

## Target

Determine whether `v` can escape to infinity without losing simple K16W
solutions.  The authorized fallback was a `lambda=1/v` boundary-stratum
classification including limiting self-contact.  A direct finite inequality
is preferable if it closes the same question.

## Results

1. **N41 (necessary implication to contradiction).** `P_-+` is empty.  A
   westward first long spoke forces `p_(2,x)>sqrt(v^2-1)`, while the prefix
   norm, host diameter and `b,c>sqrt(2)` force the reverse inequality.
2. **K31C (necessary implication).** In `P_+-`, the eastward first spoke
   confines `p_(2,x)` below `delta=v-sqrt(v^2-1)`, bounding `a`.  The westward
   second spoke similarly confines `p_(6,x)`; the final two short edges bound
   `p_(8,x)`.  Host closure then yields the explicit bounds

   ```text
   sqrt(23/2)<v<V_*<13,
   1/sqrt(2)<a<3/2,
   sqrt(2)<b,c<98/43.
   ```

3. The proof is finite and strict, so an unbounded sequence cannot evade it
   through a limiting tangency or self-contact.  The remaining bounded domain
   is still open and may approach straight bridges, boundary contact,
   tangency or singular closure; compactness alone does not make interval
   subdivision complete.

## Verification and stop

Theory note 39 gives every exact radical and rational comparison.  No limit
sample, solver, coordinate fit or formula rewrite was used.  Artifact roots
remain unchanged.  The planned blow-up is retired because the stronger
explicit bound settles its only question.  Session 147 may attempt only an
exhaustive finite strand/order theorem and a sound later-decision contract.

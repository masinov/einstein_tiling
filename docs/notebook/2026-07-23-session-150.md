# Session 150 — HC-33 corrected strand topology

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-33`, session 3 of 3

## Target

Resume D-0168 after ERR-013 using only the corrected long-strand traversal
`E,W,E,W,E`.  Derive exact vertical-order and chart reductions without
dropping a K21Q predicate or launching a formula/solver.

## Results

1. **K33M:** exact length margins prove none of the six short subpaths can
   touch the symmetry line.  The open spine has exactly five crossings there,
   namely B, C, H, C', B' in traversal order.
2. **N43:** in strand order S4, the right-half-plane B--C and H--C' subarcs
   have alternating endpoints.  Jordan separation forces an intersection, so
   S4 is empty.  S1--S3 remain unresolved.
3. **K33C:** the exact identity `r_b=Z*q*z_1`, the first-quadrant terminal
   frame and N38's `0<y_b<x_b` force `Re(z_1)<0`.  K32A therefore retains
   only first-bridge sign `sigma_1=-1`; the two positive-first-chart pairs are
   empty.
4. The corrected atlas has exactly `3 x 2=6` bounded cells.  Each retains
   closure, all 32 containment bounds, all 120 nonadjacent-pair predicates,
   the H-east sign and every discriminant/strict-boundary obligation.

## Disposition

The work is exact and theorem-only.  No cell is asserted nonempty; the six
cells are unresolved cases for K16W, not geometric or monotile candidates.
No formula, solver, coordinate fit, angle sample, polygon, SVG, patch,
decoder or aperiodicity claim was produced.

The complete suite passes: `228 passed, 17 deselected` in 140.09 seconds.
Governed artifact roots remain byte-identical to the HC-33 baseline:

```text
docs/notebook/assets  6757298033 bytes
data                  1809060890 bytes
```

HC-33 is exhausted at three sessions.  A later exact decision requires a new
checkpoint for serialization, cold equivalence validation, experiment
preregistration and D-0150-supervised solving of only the six fixed cells.

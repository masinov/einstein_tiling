# Session 159 — HC-37 selector normal form

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-37`, session 1 of 3

## Admission

This is theorem-only work under D-0182.  It will derive exact normal forms for
K36P's B and C selector polynomials and expose precisely where the fixed
bridge-chart signs enter.  It will not use a solver, CAS, numerical sample,
coordinate fit or experiment.

The session succeeds only if it proves a uniform selector sign/root reduction
or produces an exact algebraic reduction with a strictly lower admitted
variable or degree count.  A cleaner formula without a lower count is retained
as structure but is not booked as success.

## Result

K38N factors each selector into an exact line--chord incidence test.  In a
strand-aligned frame it is

```text
|w_8|^2*((T-2*n*m)^2-Q^2*(d^2-4*m^2)).
```

This makes the desired negative sign geometrically precise but lowers no
admitted complexity count.

K38B then consumes K33C's `sigma_1=-1` chart exactly.  The B supporting-line
offset is

```text
[k*a*(1-t_1^2)+2*t_1*(1-k*a)]/(1+t_1^2).
```

Its sign varies inside the single chart for every admitted `a!=sqrt(2)`; at
`a=sqrt(2)` it stays positive.  Thus the chart sign alone cannot force the B
selector.

## Verified

K38N follows by expanding `F^2-Delta*Q^2` and using
`|w|^2=s^2+n^2`.  K38B follows by taking the imaginary part of
`conj(q*z_1)*(a+q)` and evaluating its exact endpoint limits.  No research
command was run.

## Result boundary

No root or cell is removed and no variable or degree count falls.  The result
only proves that the missing inequality must consume full-cell information,
not merely the bridge semicircle.  Session 160 applies the same exact analysis
to the second bridge and C strand together with the retained strand signs.

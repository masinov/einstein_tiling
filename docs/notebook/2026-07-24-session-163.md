# Session 163 — HC-38 K28W guard-role audit

**Date:** 2026-07-24

**Checkpoint:** `HC-2026-07-24-38`, session 2 of 3

## Objective

Audit K28W before coordinates: make the proposed unequal guard sides
intrinsic, then prove exactly whether they are compatible with K9A/K9T's
four selected primary stars and complete clean spokes. This is theorem-only
work while the immutable HC-38 tangent processes continue.

## Result

Theory note 49 records three exact statements.

- K41R recovers the `e,f` roles from the rooted cyclic boundary word under
  the explicit pairwise-distinct role hypothesis. Thus boundary naming is
  not silently treated as a color.
- N44 observes that every `B,C` side in the proposed word is flanked by
  internal sides of length `d`. Since both `(B,C)` and `(C,B)` are selected
  K9A transitions, the two complete guard interfaces force both guard-side
  lengths to equal `d`.
- N45 closes K28W: genuinely unequal `e,f` contradict N44; allowing their
  collapse gives `e=f=d`, exactly the K22S rhombic family already refuted by
  N37's forced spoke crossing.

The conclusion is invariant under reflection and requires no coordinate,
angle, cover-table enumeration or solver.

## Verification

The proof is a direct incidence/length chase on the exact word

```text
e,A,d,B,d,C,d,H,d,C,d,B,d,A,f.
```

Both endpoints of both copies of `B,C` have adjacent length `d`; inserting
this into K9A/K9T equation (2.2) gives `u=v=d`. Substitution `e=f=d` into
K28G recovers K22S term for term, whose N37 no-go has already passed
independent audit.

## Decision

K28W is closed for its stated complete-clean-spoke mechanism. The standalone
K28G parallelogram equations remain a valid conditional geometry, but they
cannot be promoted to the compiler language. Session 164 must choose a new
carrier topology by changing a named N44 hypothesis; it may not try another
`e,f` value, coordinate fit or solver.

The six HC-38 tangent cells remain running unchanged. This theorem result
does not modify their formulas, option set, budgets or certificate boundary.

# Session 09 — 2026-07-16 — A6 finite forcing gate

## Done

1. Audited the v1 collared rule table before treating it as a forcing
   language.
2. Replaced independently numbered parent/child collar colors with one
   normalized stationary alphabet aligned by exact graph isomorphism.
3. Ran both locally exact 9/8 physical phases through recursive closure.
4. Enumerated stable radius-1 physical-tile collars and all legal parent
   patterns of the recursively closing phase.
5. Added CaDiCaL composition checks at the physical and metatile levels.
6. Regenerated the A6 JSON artifact and promoted M5's computational v0 gate
   to complete.

## Important audit correction

The v1 artifact reported 17 parent classes and 17 child classes, but those
integers had been assigned independently from scale-local exact signatures.
Their domains differed:

- old parent rule keys:
  `0,3,5,7,9,11,13,14,15,16,18,19,21,24,26,28,29`
- old child values:
  `0,2,4,7,9,11,12,13,15,17,19,20,24,25,26,28,29`

Equal cardinality was therefore not closure. The parent contraction is now
mapped to the previous patch's equal-sized child contraction by the exact
20-round refinement bijection. Same-scale child signatures match directly.
The corrected substitution uses states `0..16` on both sides, is deterministic,
closed, and strongly connected.

## Physical phase selection and forcing

The immediate pose-only miner still honestly finds two exact-cover phases on
both N=3 and N=4. No heuristic is needed to trust one:

| phase | missing scaffold edges (internal/exposed) | recursive result |
|---|---:|---|
| selected | 34 / 44 | unique closure `496→63→8→1` |
| alternate | 32 / 48 | rejected: two exceptional-child compositions survive |

The old cohesion ranking agrees with this result but is now diagnostic only.

For local recognition, radius-1 physical edge neighborhoods stabilize exactly
between N=4 and N=5:

| quantity | result |
|---|---:|
| physical collar states | 32 |
| legal selected-phase parent patterns | 19 |
| geometric 9/8 occurrences from both phases | 11,715 |
| admitted occurrences | 3,905 |
| rejected competing occurrences | 7,810 |
| admitted occurrences outside selected cover | 0 |
| SAT pattern cases | 19/19 unique |

Thus recursive closure selects the language and a finite radius-1 physical
rule recognizes it.

## Stationary metatile language and SAT

- normalized states: 17 parent = 17 child;
- deterministic rules: 17/17;
- transition graph: strongly connected;
- fully collared parent samples: 310;
- visible two-type (8/7) composition: 310/310 SAT-unique;
- radius-1 fully colored candidates: 620 total, 310 legal and 310 rejected;
- legal candidates outside the known cover: 0;
- complete finite-patch contexts: 309/309 SAT-unique;
- all 17 state cases represented (one remaining eligible sample touches an
  incompletely collared alternative at the finite boundary, while its state
  has 29 other complete representatives).

Withheld ancestry still matches at every depth, and the 17 states remain pure
against all nine supplied labels. Neither oracle is consumed during discovery.

## Decision / scope

D-0014 records the corrected stationary alphabet and forcing semantics. M5 is
complete as a computational calibration gate and Gate G1 is unblocked. The
JSON is a verified finite case table, not yet a Lean theorem over infinite
tilings; formal wrapping belongs to E10. The traditional named two-tile
Gamma/Mystic fusion is not needed by the recovered 9/8 certificate.

## Verification

- `scripts/run_a6_spectre.py`: **PASS**
- fast Python suite: **57 passed, 14 deselected** in 8.70 s
- slow Python suite: **14 passed, 57 deselected** in 112.17 s
- vendored Rust suite: **5 passed**
- artifact: `docs/notebook/assets/a6-spectre-results.json`

## Next

1. Add an exact A3-polykite-placement → A6 module-pose/boundary adapter.
2. Require the blind hat patch to recover a hierarchy through A6.
3. Scale A0/A1/A2 for the full frozen-threshold E1 sweep through n≤16.

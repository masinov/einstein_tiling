# Session 80 — the `L/2L` phase needs a local anchor

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-08`, session 2 of at most 3

**Mode:** on-paper gauge analysis only; no polygon, contact census, search,
drawing, experiment or generated artifact

## Question

Can the triangular lattice's index-four quotient supply K2H's hidden phase
without an external origin?

## N8

The bare triangular frame is invariant under every translation in `L`. Any
translation-equivariant local output must inherit those periods. Its absolute
`L/2L` coset coloring is changed by primitive translations and has only `2L`
as its period lattice. Hence no local rule from the bare frame selects it.

Equivalently, integrating quotient-valued edge increments determines phases
only up to a global additive constant. A carrier may defeat that ambiguity
only with additional bounded geometry that anchors one of the four classes.
Then the bounded star, rather than `L/2L` alone, is the mechanism.

## Decision

The unanchored translation-phase option is closed. The final HC-08 session
will decide whether the abstract hidden-state factorization reduces any
geometric burden in the absence of an independently observable central
feature, and will state the remaining K2G route exactly.

No `EXPERIMENTS.md` entry is added.

## Verification

- the period-equivariance calculation is exact;
- primitive vectors in `L` act nontrivially on `L/2L`;
- the scope is the bare/unanchored frame and does not exclude contextual
  contact-star anchors;
- `git diff --check` and governance tests passed;
- no research command or generated artifact was created.

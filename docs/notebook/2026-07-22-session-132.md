# Session 132 — prefix cone and three-rotor closure elimination

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-26`, session 2 of 3

**Mode:** on-paper exact elimination; no experiment

## Proposition

Couple the fixed first corner and bridge variables far enough to obtain a
complete exact K16W witness or a scoped K16B incompatibility theorem.  A
relaxed or partial condition is not a terminal outcome.

## Result

K19P writes the complete two-vertex corner-prefix cone and proves the exact
necessary bound `a>u/sqrt(2)`.  The unequal rectangle

```text
(u,v,a)=(1,2,1),       r_0=(24/25,7/25)
```

passes the full prefix cone exactly.

K19E rewrites the endpoint as three independently rotatable fixed vectors,

```text
p_8=(a+u*q)r_0+(v+b*q+u*q^2)r_b+(v+c*q+u*q^2)r_c,
```

and eliminates their phases from host closure.  Closure alone is equivalent
to intersection of one exact vector-sum annulus with the radial interval of
the central-chord circle.  The positive unequal tuple
`(u,v,a,b,c)=(1,2,1,1/10,1/10)` passes that relaxed test exactly and also
passes K19P.

It has not been shown to satisfy either K18H cone or simultaneous K17S
containment.  It is not a witness.

## Decision

The prefix and closure relaxations both survive.  No terminal HC-26 outcome
has been reached.  Session 133 is the last authorized session and must either
prove all K16W predicates for one exact simple spine, prove a scoped K16B
incompatibility theorem, or fire the freeze and name the already-selected
non-right-guard pivot.

## Verification

- K19P is the direct first-two-edge expansion of K17S.
- Its exact control uses only `24*sqrt(2)>31` and
  `17<18*sqrt(2)`, verified by squaring positive sides.
- K19E follows from the exact three-vector polygon interval and the radial
  interval of a circle centered at `D/2`.
- The closure control uses `sqrt(5)<9/4`; no decimals or sampled angles occur.
- Reflection and the degenerate-line branch remain closed by K18R/N35.
- No research command or generated artifact was produced.
- Artifact roots remain byte-identical to the HC-26 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.


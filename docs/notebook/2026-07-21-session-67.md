# Session 67 — ST-M1 source correction before the collar table

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-04`, session 1 of at most 3

**Mode:** primary-source and proof-audit work only; no experiment, runner,
enumeration, shape construction, or generated artifact

## Authorization and fixed stop rule

After independent review of sessions 64--66, the user explicitly authorized
the proposed 38-address collar/port table. The contact-corona radius was fixed
at one in D-0075; failure at that radius was to be reported rather than
answered by escalation.

Before listing states, the session checked the proposed raw templates against
the cached primary TeX. This prerequisite check refuted the table's premise,
so the work stopped before a finite classification or any code was written.

## Primary-source finding

Table 1 of Akiyama--Hamada--Ito distinguishes two `sqrt(2)-1` examples:

| construction | Type I composition | Type I count | Type II |
|---|---:|---:|---:|
| earlier Section 9 | `2S+L` | 26 | `M` |
| optimized Section 10.1 | `12S+6M+6L` | 2 | `M` |

Session 65 took the `2S+L` composition of the earlier construction, multiplied
its visible `S/L` patches by six, and assigned the resulting 18 cells to each
optimized large prototile. It thereby omitted the attached `M` cells stated in
the optimized row. The `18,18,2` and 38-address claims are invalid.

The later sentence that supports cease to differ at `kappa=infinity` appears
in the separate Smith Turtle subsection. The paper does not instantiate it on
the optimized system or supply a common-cell subdivision, complete SAB/vertex
atlas, or all-tilings language equivalence for the `12S+6M+6L` templates.
Changing 38 to a guessed larger count would not prove E-infinity.

The source's preceding statement that at least two tiles are required for a
quadratic-slope Sturmian lattice is also now scoped explicitly. It concerns
the paper's colored tile-set category. ST-M1's proposed contextual states
would be decoded from a one-support unmarked tiling and so would lie outside
that category; the distinction avoids a contradiction but does not provide a
construction.

## Mathematical disposition

- ST-M1.S0C survives as a conditional common-cell compiler.
- ST-M1.E-infinity and the instantiated ST-M1.S0 return to `blocked`.
- The all-`M` endpoint exclusion is not proved; it depended on the nonexistent
  transported collar atlas.
- Q0 remains a correct conditional period-descent criterion.
- N1 and N2 remain correct conditional no-go results for independent rails and
  the unrestricted `S/M/L` role system.
- K1 is blocked upstream of any address/collar table.

The correction is ERR-006 and D-0076. The stable theory notes, literature
review, methods matrix, proof ledger and status page now use those entries as
the controlling state. Sessions 65--66 retain prominent correction notices
as historical failed-proof records.

## Decision and stop

No radius-one table was constructed, so the fixed-radius stop rule was met
without escalation. Reopening this branch requires an exact
source-independent common-support construction for the actual two
`12S+6M+6L` templates and `M`, including connectedness, complete local-language
transport and irrational corridor decoding. A revised address count alone is
not an admissible next step.

No `EXPERIMENTS.md` entry is added. The artifact roots were untouched.

## Verification

`git diff --check` and checkpoint JSON parsing pass. The targeted governance
suite passes `4/4`; the complete fast suite passes `222/222` with 17 slow
tests deselected in 145.52 s. Artifact roots remain byte-for-byte at the
checkpoint baselines: `6,756,125,300` bytes under `docs/notebook/assets` and
`1,793,649,957` bytes under `data`.

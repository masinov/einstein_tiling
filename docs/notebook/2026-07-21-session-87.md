# Session 87 — adversarial ST-M1 integration audit

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-10`, session 3 of 3

**Mode:** documentation and proof-scope audit only; no experiment, source
reconstruction or generated artifact

## Result

The consolidated theory note survives an adversarial dependency audit at its
declared proof-draft level. Its conditional theorem starts only after all K2J
geometric obligations, treats reflected tilings explicitly, and does not infer
surjectivity or entropy. Its source dependency remains explicitly conditional
on the non-serialized S0 presentation.

The monograph outline, theory index, literature methods matrix and
research-return audit now route readers through theory note 14 and name SER0
and K2J as independent blockers. The stale n=10 outline is also corrected to
identify the case study as the published Turtle rather than an unresolved
finalist.

## Adversarial checks

- P0 is used only as a composition selector, never as a source-language
  theorem.
- S0 remains a proof draft and SER0 remains blocked; prose consolidation does
  not create a machine-verified atlas.
- K1P fixes its witness/type and extends the parity core with distinct fresh
  diagonal tags.
- N5 and N6 retain their restricted arity/product-box scopes; N7--N9 do not
  exclude contextual geometric states.
- K2C projects to the complete selected codebook, while K2V deliberately
  retains six sectors rather than imposing an unjustified shared vertex bit.
- The K2J conclusion is conditional on contact completeness, chirality and a
  witness lift; no polygon is claimed.
- “No monotile exists” was replaced in the methods matrix by the unambiguous
  “no monotile construction or result is established.”
- The full suite exposed a malformed `SOURCES.json` `review_note` containing
  prose plus two paths. It now names the actual review file; that review links
  the separate serialization contract.

## Verification

- `git diff --check`;
- governance tests;
- complete fast test suite: `222 passed, 17 deselected`;
- artifact roots are byte-identical to the HC-10 baseline:
  `docs/notebook/assets = 6,756,125,300` and
  `data = 1,793,649,957` bytes.

## Disposition

HC-10 is exhausted. ST-M1 remains frozen. It may reopen only with exact SER0
input/reconstruction or an on-paper K2J gadget satisfying the existing
contract; no experiment or geometric search is authorized by this session.

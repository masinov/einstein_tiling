# Session 172 — exact Sturmian source reconstruction

**Date:** 2026-07-27

**Checkpoint:** `HC-2026-07-27-42`

## Objective

Reconstruct the finite normalized supports and boundary/SAB incidence of the
three Section 10.1 Akiyama--Hamada--Ito patch tiles. This closes a source
obligation of the controlling Sturmian-monotile theorem; it does not search
for a carrier.

## Experiment pre-registration

### Proposition

Determine whether the analytic line/centroid definitions and the pinned
vector figures uniquely reconstruct a finite exact normalized source atlas,
beginning with the claimed `30,30,2` primitive-triangle supports and their
boundary words.

### Prior art and non-redundancy

The primary arXiv source archive is pinned by SHA-256
`de757bfc8e3fe174fc04dd19101f30e13dc6776d245f573ff9554f23a60bad28`.
It supplies Illustrator vector figures but no coordinate, address, SAB or
vertex tables. SER0 records this exact missing artifact; reconstructing it is
not a repetition of a published table.

### Outcome decisions

- A unique reconstruction that matches both analytic definitions and every
  source count/figure becomes the finite source input for the total-decoder
  problem.
- An ambiguity or inconsistency is recorded explicitly and blocks geometric
  synthesis; it is not resolved by silently choosing a drawing interpretation.

### Stop rule and finite justification

The input is fixed to `Example1.pdf` page 1, `Isometric.pdf` pages 1--5 and
the corresponding Section 10.1 definitions. Stop after the three supports
and their boundary/SAB data are classified once. No radius, corpus, solver or
figure set may be enlarged in this run.

### Human checkpoint

`HC-2026-07-27-42`; no large artifacts are planned. Any extraction program
must run through `scripts/run_research.py` after this note passes the gate.

## Results

The governed extraction produced
`data/sturmian-source/ahi-section10-supports.json` (SHA-256
`50f2d948623c7f6d1c802a7606892e463932a66560882dad65910c1816d53afd`).
The cold verifier establishes:

- exact primitive-triangle counts `30,30,2`;
- the two 16-edge large boundary words and four-edge small word;
- SAB path census `15,15,1`;
- role census `6 S-pairs + 6 M + 3 L-pairs` in each large support;
- every SAB component pairs exactly two adjacent triangles and the components
  partition each support;
- the resulting common `60/120` rhombus macro graphs are connected, with
  address counts `15+15+1=31`;
- the two role-labelled SAB embeddings on either large support form one orbit
  under its exact support stabilizer.

The first launch stopped before writing an artifact because the provisional
Illustrator direction tolerance was too narrow.  The repaired transcription
uses a wide exact rational separation between vertical and 30-degree source
cones; all serialized geometry and all certificate checks after
transcription are integer-exact.  The final producer and cold verifier pass,
as do the targeted governance/source tests.

This closes the finite SER0 support/SAB transcription.  It does not erase the
31 colors or prove the no-spurious-tilings lemma for one unmarked polygon.

# Session 127 — complete K10B square-lens no-go

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-24`, session 2 of at most 3; closed early

**Mode:** on-paper exact inequality elimination; no experiment

## Proposition

Solve K15S jointly with the K13A and `d`-cover arithmetic, or prove a scoped
incompatibility theorem without enumerating weight tuples.

## Result

N33 proves the K15S feasible set is empty before U1/U2 enter. After scaling
`d=1`, strict containment supplies positive margins

```text
alpha=a-1/sqrt(2),
beta=b-(sqrt(2)-1),
mu=sqrt(2)-h,
c=1-1/sqrt(2)-alpha-beta-mu.
```

Writing `t=tan(x)`, positivity of the fourth rotated vertex forces `0<t<1`.
Its upper second-coordinate bound requires `beta>L(t)`, while the sixth
vertex's upper first-coordinate bound requires `beta<U(t)`. Exact squaring
gives

```text
L(t)>U(t),
2*(positive-square difference)
  =(1-t)*(t^3+5*t^2+5*t+1)>0.
```

The two containment intervals are disjoint for every allowed orientation.
Thus no positive `(a,b,c,d)` with `h=a+b+c` realizes the unchanged K10B
square-lens spine.

## Decision

HC-24's incompatibility branch fires in session 127. K13A and K15D survive
as arithmetic tools, but changing weights alone cannot repair K10B. A future
geometric branch must name a changed lens, boundary prefix or angle
hypothesis before work begins.

## Verification

- The normalized margin identity follows exactly from `h=a+b+c`.
- Only the already-complete `w_4,w_6` inequalities of K15S are used.
- The polynomial in the square difference has strictly positive factors on
  `0<t<1`.
- No parameter enumeration, coordinate fitting, experiment or artifact was
  produced.
- Artifact roots remain byte-identical to the HC-24 baseline:
  `docs/notebook/assets=6756125300`, `data=1809060890` bytes.
- Repository tests: `222 passed, 17 deselected` in 145.47 seconds.

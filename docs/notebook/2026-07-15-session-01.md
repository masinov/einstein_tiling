# Session 01 — 2026-07-15 — Founding: repo, kite substrate, A0, hat anchor

## Starting state

Repo contained only `einstein_search_program.md` (untracked), README, LICENSE,
.gitignore, an empty Python 3.12.7 venv. The "spectre engine" the program
references as its v0 kernel is **not present** — everything is built from
scratch here.

## Done

1. **Repo organization.** Program document moved verbatim to
   `docs/program/`; created STATUS / EXPERIMENTS / DECISIONS / ERRATA /
   notebook structure; `src/einstein` installable package (`pip install -e .`),
   pytest wired (`pyproject.toml`).
2. **External anchors fetched** (before writing any geometry code):
   - OEIS **A057786** (free polykites), terms n=1..25, via OEIS JSON API.
     Independent source: computed by Brendan Owen, extended by C. Kaplan from
     J. Myers' tables.
   - The hat's outline (13 vertices, hex coordinates), verbatim from Kaplan's
     hatviz reference implementation (`geometry.js`, `hat_outline`,
     github.com/isohedral/hatviz). Confirmed from arXiv:2303.10798 that the
     hat is an **8-kite** polykite → program doc error recorded as
     [ERR-001](../program/ERRATA.md).
3. **Kite substrate** (`src/einstein/substrate/kitegrid.py`): Laves [3.4.6.4]
   grid, hexagon side 2, all geometry in exact integer hex coordinates
   (see [D-0003](../DECISIONS.md)). Cells (cx,cy,d); adjacency derived
   analytically and verified geometrically by tests (shared-edge checks,
   exact-cover check on a 6-ring patch, transform-vs-geometry agreement for
   all 12 point-group ops).
4. **A0 enumeration** (`src/einstein/enumeration/polyform.py`): free
   polykites by grow-and-canonicalize BFS.
5. **SVG renderer** (`src/einstein/render/svg.py`) — §10 "Smith factor"
   channel; floats only at render time.

## Verified (evidence)

- 14 tests green: `venv/bin/python -m pytest` + `-m slow`.
- A0 counts == OEIS A057786 **exactly, n=1..12**:
  1, 2, 4, 10, 27, 85, 262, 873, 2917, 10011, 34561, 120815.
  Timing (single core, Python): n=10 cumulative 2.4 s, n=12 cumulative 40 s.
- Hat end-to-end: outline embeds with integer coordinates; exactly 8 kite
  cells recovered by exact point-in-polygon
  (cells: `[(0,0,0),(0,0,1),(0,0,4),(0,0,5),(2,2,3),(2,2,4),(4,-2,1),(4,-2,2)]`);
  union boundary reproduces the outline exactly (collinear-normalized cyclic
  comparison); area = 32 shoelace units = 8 kites; canonical form present in
  the enumerated 8-cell free polykites. Render:
  [assets/hat-recovered.svg](assets/hat-recovered.svg).

## Failures / corrections during the session

- First version of `test_ops_form_group_of_order_12` wrongly asserted that a
  same-hex adjacent kite pair is chiral; it is mirror-symmetric (6 images,
  not 12). Test fixed to use the chiral sector set {0,1,3}. Substrate code
  was correct; the error was in the test's expectation.

## Open / next (carried to STATUS)

- M1 = A1 periodicity rejection (torus/exact-cover first, then isohedral),
  SAT solver dependency decision pending.
- Shape database (§7.4) minimal SQLite schema before A1 emits verdicts.
- A0 scaling: streaming/compiled enumerator needed beyond n≈14 (E1 needs 16,
  E2 needs 22–24).
- Rank-4 ℤ[ζ₁₂] module: deferred (D-0003) until 12-fold work needs it.

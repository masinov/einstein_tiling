# Session 103 — HC-16 selected-word angle selector

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-16`, session 1 of at most 3

**Mode:** on-paper exact geometry; no experiment

## Proposition

Before proposing coordinates, decide whether fixed code-side endpoint angles
can select exactly two non-reversal orders of three unequal neighbors while
using ordinary three-participant internal junctions.

## Prior art and non-redundancy

HC-15 already established the edge-patch prior-art boundary and J0. This
session only solves the finite angle equations for two selected K6O states.
No novelty claim is made for edge patches, local matching rules or symbolic
compilers.

## Result

K7A gives a complete normal form. Any two reversal classes can be written
`ABC` and `ACB`. Their four J0 equations force

```text
ell_B = ell_C = theta,
rho_A = rho_B = rho_C = pi-theta.
```

The unused `ell_A` endpoint is free. Setting it different from `theta`
excludes exactly the third class, because that class puts `A` in the middle.
The exact orthogonal specialization

```text
A=(3*pi/2,pi/2), B=C=(pi/2,pi/2)
```

therefore admits precisely two states and forces nonconvexity at the named
reflex endpoint.

This is an infinitesimal-sector result only. It supplies no polygonal disk or
nonoverlap proof.

## Decision and stop rule

Sessions 104--105 may seek only an irredundant exact orthogonal polygon with
pairwise-distinct code lengths summing to its host and explicit `ABC`,`ACB`
patches. Coordinates must precede any verification, and coverage and
disjointness must be hand-checkable. Failure by session 105 freezes this
route; no search, optimizer, SVG or candidate promotion is authorized.

## Housekeeping and verification

- Recorded the explicit HC-16 authorization and its fixed escape route in
  `HUMAN_CHECKPOINTS.json` at the verified session-102 artifact baseline.
- Removed the reviewed zero-byte WebFetch fragment. The verified PDF beside
  it was not changed.
- No research runner or generated research artifact was created.

# Session 196 — torsion-free no-go and exact parity zipper

## Done

- Proved K70A: no finite collection of participant-wise additive tests in a
  two-torsion-free abelian group can distinguish ternary even parity from the
  full binary cube.
- Applied it as N70T to close fixed-order/fixed-multiplicity host interfaces
  based on lengths, angles, areas, displacements, or vector closure.
- Constructed K70Z, an exact rooted non-edge-to-edge contact complex with
  four equal-length directed roles and explicit endpoint angles whose
  T-junction fills enforce a two-state phase zipper.
- Proved that its three visible bits are exactly the four even-parity words
  and that four directed code roles are minimal in this phase-continuity
  model.
- Stated K70P, the six-clause one-connected-polygon and all-tilings admission
  contract.

## Verified

- Subtracting the four even additive equations gives
  `d_B+d_C=d_A+d_C=d_A+d_B=0`; absence of two-torsion forces all differences
  to zero.
- The exact angle table gives matched sums `pi` and mismatched sums
  `11pi/12`, `13pi/12`.
- Telescoping `x_i=q_(i-1) xor q_i` with `q_0=q_3=0` proves even parity, and
  integration proves a unique lift for every even word.
- The even length-three words use every transition `00,01,10,11`, proving the
  four-role lower bound under rooted endpoint-phase recognition.

## Failures / boundaries

- K70Z still has rooted phase-zero terminal data.  No unmarked delimiter,
  cap, or closed carrier contact cycle has yet forced those terminals.
- No single polygon has been shown to contain the host and four directed
  roles or to realize the four complete patches without overlap.
- No unintended-contact atlas, whole-plane lift, periodicity result, or
  monotile claim exists.
- No novelty claim is made for the torsion or finite-transducer algebra.

## Open

Solve K70P.  The first exact question is terminal erasure: can one connected
boundary force phase zero at both ends of the three-code-side subdivision
without colors, while preserving all four parity words and excluding every
odd or imitating cover?

No experiment, enumeration, solver, coordinate search, SVG, or candidate was
opened.

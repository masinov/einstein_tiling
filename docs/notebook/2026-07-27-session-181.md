# Session 181 — validate the 17-rhombus source compiler cell

## Experiment pre-registration

### Proposition

For every K54R minimum equalizer, form the two legal-tile decompositions

```text
large_A + 2 singleton_M   and   transformed large_B + 2 singleton_M.
```

The singleton occurrences occupy the two rhombi missing from that macro,
irrespective of the role assigned to the same support by the opposing macro.
Decide exactly whether every new internal contact continues the SAB at the
same shared-edge endpoint, whether both decompositions induce the same outer
SAB boundary signature, and whether the two decorated decompositions remain
distinct under the full support-isometry group.

### Prior art and non-redundancy

K54R proved only support equality and K54S rejected the construction from the
opposing macro's role labels.  The AHI matching rule is instead edge-to-edge
plus SAB continuation; a legal singleton `M` may occupy any congruent rhombus
if those actual contact rules hold.  This run tests the omitted source-language
condition and may supersede K54S.

### Outcome decisions

- If both sides are legal and have the same outer signature, promote the
  17-rhombus disk to a fixed source compiler kernel.  If its decompositions
  are full-isometry-distinct, it supplies an intrinsic binary macro state;
  otherwise it requires a rooted context.
- If either side violates a contact or the outer signatures differ, retain
  K54S with the exact failing edge as its stronger certificate.

### Stop rule and finite justification

Use only the four serialized K54R equalizers, their 17 rhombi, and the exact
SAB endpoint rule already reconstructed from all 44 known macro contacts.
Check every internal and boundary edge once.  Do not search another support,
alter a boundary, or infer legality from role names.

### Human checkpoint

`HC-2026-07-27-45`, session distance one.  This directly decides whether the
smallest exact common support is a legal source compiler cell.

## Results

All four minimum equalizers form the same 17-rhombus disk.  On both sides,
all 26 internal contacts continue at the same collapsed SAB endpoint and the
complete 16-edge exposed endpoint signatures agree.  The two large groupings
are not exchanged by a support automorphism.  This refutes K54S's role-only
rejection, subject at this session to the explicit full-germ audit in session
185.

## Verification

The cold verifier rebuilds all four equalizers from the source atlas and
common-support kernel.  Artifact:

```text
data/sturmian-source/ahi-seventeen-rhombus-compiler.json
sha256 bfdaa83507e5a25b3bb989e17b1787f04b0f37c80035e8daaab20f5c6560e097
```

## Decision

Retain the fixed polygon for the source-rule calibration.  Do not infer
whole-plane tilability or total grouping.

# Session 90 — binary retiling kernel and HC-11 stop

**Date:** 2026-07-21

**Checkpoint:** `HC-2026-07-21-11`, session 3 of 3

**Mode:** bounded on-paper mechanism attempt; no experiment, shape search or
generated artifact

## Result

The square-diagonal kernel gives an exact contextual-state mechanism: two
right-isosceles copies retile one square along either diagonal. Under an
exclusive hypotenuse-pairing and complete leg/corner guard, every physical
tiling would group uniquely into binary square macrocells. Diagonal ownership
is visible on macro edges and at four-square corner stars, so the projected
language can have genuine two-dimensional plaquette coupling.

The straight triangle is only a periodic control. No polygonal guard is known
to exclude every partial contact and fault tiling while preserving both
retilings. Nor has a binary plaquette language with a total decoder to K3F/S0
been supplied. B0 names that upstream symbolic obligation.

## Kill-condition decision

HC-11 required an exact boundary mechanism with finite contact completeness
before a shape run. The diagonal kernel supplies the mechanism topology but
not the load-bearing guard theorem. The kill condition therefore fires:

- no polygon is promoted;
- no boundary enumeration or contact experiment is admitted;
- K3G remains frozen;
- K3B/B0 are retained as a sharply constrained future route rather than a
  candidate claim.

## Verification

- both two-triangle square retilings are exact by direct coordinates;
- exclusive `H` pairing implies unique pair grouping because every occurrence
  has degree one in the `H`-contact graph;
- the proof explicitly assumes rather than infers leg/corner contact
  completeness;
- binary state capacity is not confused with a binary-to-source decoder;
- complete fast suite: `222 passed, 17 deselected`;
- artifact roots remain byte-identical to the HC-11 baseline:
  `docs/notebook/assets = 6,756,125,300` and
  `data = 1,793,649,957` bytes;
- no research computation or generated artifact was used.

## Next decision

HC-11 is exhausted. Before geometry, audit the primary literature on strongly
aperiodic binary square-plaquette SFTs and binary higher-block simulation, and
decide whether B0 is mathematically plausible. If it survives, a later
checkpoint may preregister a fixed-`N` exact boundary-word synthesis with an
explicit contact-completeness certificate.

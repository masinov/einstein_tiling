# Session 33 — factor Layer D to a local SFT and find its exact limit

**Date:** 2026-07-18
**Scope:** derive the two-bit local form of the A4 obstruction, falsify the
index-55 signature at the next shell, and explain the escape as an infinite
pullback family.

## Four-colour local coverability SFT

After T2.D4 normalizes the C3 potential to `chi(x,y)=2x+y mod 3`, only a V4
colour remains at each quotient vertex. A selected edge imposes

`v_end = v_deck + v_start + M^chi(start) v_label`.

Thus an available tile placement is one whose boundary colours satisfy a
fixed affine pattern; every cell needs at least one available covering
placement. The new encoder uses two bits per vertex. It reproduces all 336
certified index-55 map-7/V4-twist cases.

Artifact SHA-256:

- `theory-w2-layer-d-v4-sft-index60.json`:
  `fccd728e580d6d77b831334342b7b3cdd3b0541298f65de15a6d136393b48292`
- `theory-w2-layer-d-v4-signature-index60.json`:
  `af987ca05eed0c76e87ac42ff21a44ed53dc5338f2942d68e1c84ad145b24d61`

## Index-60 falsification

W1 leaves 45 HNFs. Map 7 obstructs 42 under all 16 V4 twists. The three
escapes `(10,2,6)`, `(30,6,2)`, `(30,22,2)` form one D6 orbit. All 16
distinct-tail maps escape on all three, each with exactly one rank-two twist.
The complete certified prefix therefore remains 55.

## The 2-Lambda countermodel family

All three escape lattices lie in `2 Lambda`. Every signature map has one
explicit SAT assignment on the base HNF `(2,0,2)`. Restricting its V4
holonomy and pulling back placements/colours gives a model on every HNF
`(a,b,d)` with all three entries even. A cold verifier checked all 16 base
witnesses and 3,024 map/HNF lifts over the 189 even HNFs through index 60.

Artifact `theory-w2-layer-d-v4-2lambda.json`, SHA-256
`21f021f8d3f20a38d1c885ebc70ac260b068b7b692e73de569e3688b9768f556`.

## Products do not repair the relaxation

The product of all 16 signature layers is SAT on `(2,0,2)` with one shared
set of three placements. Eighteen cells are covered once and six twice.
Every pair is therefore SAT by projection, overlap-at-most-two is SAT, and
the full product lifts clausewise to all three index-60 escapes.

Artifact `theory-w2-layer-d-v4-product.json`, SHA-256
`62aae01fe772401bb0e9a9af4305e181ddb53e151c423413583f156e59e0e278`.

This is a no-go theorem for the relaxation, not a periodic tiling: the models
overlap. The correct next Layer-D obligation is packing/nonoverlap or an
equivalent density constraint. Merely trying larger finite products is now a
retired branch.

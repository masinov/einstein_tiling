# Session 107 — HC-17 uniform mismatch algebra

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-17`, session 2 of at most 3

**Mode:** on-paper finite length algebra; no experiment

## Proposition

Determine whether the four directed adjacencies in `ABC,ACB` can expose one
recognizable positive stem excess with the same cap orientation.

## Prior art and non-redundancy

N22 already forces the secondary participant. This session solves only the
finite equality system for a uniform socket and makes no matching-rule,
T-junction or compiler-novelty claim.

## Result

K8U gives the unique solution up to reflection:

```text
r_A=r_B=r_C=s,
l_B=l_C=s+Delta,       s,Delta>0.
```

Every allowed adjacency therefore has the left occurrence end first after
`s`, while the right occurrence exposes the same continuation `Delta`.
Each selected host word creates two identical right-angle sockets. A cap
using the complete continuation needs one side of intrinsic length `Delta`.

This solves the length algebra only. The common lower endpoint of the longer
stem and cap side still needs a finite contact cycle; otherwise N22 repeats.

## Decision and stop rule

Session 108 is the final HC-17 session. It may derive a finite exact local
cycle closing the lower endpoint with the admitted occurrences, or it must
freeze the route. No coordinate search, polygon, SVG, contact-atlas
enumeration or candidate promotion is authorized.

## Verification

- K8U is checked by direct substitution in the four adjacency equations.
- `git diff --check` and the governance test are run before commit.
- No research runner or generated research artifact was created.

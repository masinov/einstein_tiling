# Session 114 — role-pairing correction and HC-19 stop

**Date:** 2026-07-22

**Checkpoint:** `HC-2026-07-22-19`, session 3 of 3

**Mode:** on-paper geometry audit; no experiment

## Proposition

Close the fixed K10B spine to exact coordinates and both complete shield
patches, or fire the HC-19 stop without widening the design.

## Correction found before promotion

The full-spine half-turn pairs interior angles to `2*pi`. Therefore the two
equal-length copies of each `A,B,C` side are not one reversal role: the
intended copy has convex K9A endpoints and the mirror is a reflex auxiliary
side. ERR-008 and N26 record the correction. The intended side is still
locally recognizable by length plus endpoint context, but contact
completeness must control the auxiliary side separately.

## Result

No exact vertex list satisfies all six K10B obligations. The centrally paired
length word and half-turn isometry remain exact, but an actual simple spine
inside the right-angle lens was not simultaneously reconciled with all five
sharp code/spoke angles and both complete eight-occurrence host patches.
Local guard/shield docking alone is not treated as a patch witness.

## Decision

The HC-19 stop fires. K10B remains a conditional finite reduction; N25 and
N26 remain exact no-go/recognition filters. No polygon, candidate, SVG,
enumeration or optimizer follows. Reopening requires exact coordinates and
both placement tables in advance.

## Verification

- N26 is the direct `2*pi` sector sum at every paired nonterminal spine
  vertex.
- `git diff --check`: clean.
- Full fast suite: `222 passed, 17 deselected`.
- Artifact roots remain `docs/notebook/assets = 6,756,125,300` bytes and
  `data = 1,809,060,890` bytes.

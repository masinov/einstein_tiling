# Session 140 — HC-30 bridge-dependent crossing cells

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-30`, session 2 of 3

## Proposition

Complete the exact intersection partition for the third critical spoke pair:
the two length-`v` segments in K16B's `B` and `C` hooks. Keep both bridge
angles symbolic and consume N38's aspect/polarity cone.

## Results

Let `R=r_c/r_b=X+iY` and write

```text
A=v-b/sqrt(2),   B=b/sqrt(2)-u.
```

N38 gives `A,B>0`. K27X proves that the two closed long spokes intersect
exactly when all three linear conditions hold:

```text
vY+B<=0,
AY-BX<=0,
(A-v)Y-BX>=0.
```

Their disjoint complement is the exact three-cell union C0--C2 in theory
note 35. The formula depends only on the relative second bridge phase
`q^2*z_2`; the terminal orientation and first bridge cancel.

Together, K26X and K27X completely classify the three critical spoke pairs:
two rigid-hook pairs and the pair of long spokes. A simple K16W point must
lie in N38's one wide aspect cone, one of four polarity cells, and one of
three K27X cells—at most twelve named semialgebraic cells.

## Exact checks

- Dividing `t*v=H_b+s*v*R` gives
  `s=-B/(vY)` and `t=(AY-BX)/(vY)`.
- Under `0<=s<=1`, `Y<0`; reversing inequalities over the negative
  denominator gives exactly the last two signs above.
- At `Y=0`, the lines are parallel with normal offset `B>0`, so C0 safely
  includes this case.
- The safe cells are disjoint by their first failed inequality and exhaust
  the logical negation of the closed intersection conjunction.

## Decision

HC-30's primary decomposition outcome is met. This is stronger input for a
future decomposed exact decision, not a solver verdict: K16W remains open and
frozen because 117 noncritical segment predicates, closure and complete
containment still have to hold simultaneously. No run or artifact was
created. The single remaining HC-30 session may perform only the authorized
secondary unequal-guard transfer assessment; it may not launch a K16W
solver.

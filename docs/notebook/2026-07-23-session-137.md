# Session 137 — exact non-right closure and simplicity freeze

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-28`, session 3 of 3

## Question

Does complete containment in K23I's surviving interval force the host length
above the rhombus diameter, or can one construct a complete exact simple
K22S spine?

## Exact result

The proposed diameter implication is false. K24C uses the already selected
rational angle `cos(gamma/2)=4/5`, `sin(gamma/2)=3/5`, the rational unit
direction `(24,7)/25`, `a=3/4`, `b=1/2`, and

```text
c=11377/7500 - 2*sqrt(1586086)/1875.
```

The root lies strictly in `(1/6,7/40)`. Every first-half oblique coordinate
is consequently in `(0,1)`, and central closure reduces exactly to

```text
150000*c^2-455080*c+74471=0.
```

Thus non-right geometry escapes N33 at containment plus closure.

N36 rejects this exact control as a carrier: nonadjacent segments `p_1p_2`
and `p_3p_4` cross with interior parameters `7/12` and `5/12`, at oblique
point `(5/12,7/12)`. This is independent of the algebraic coordinate `c`.

## Process correction

ERR-011 records that a transient read-only diagnostic sampled a small table
of floating angle pairs while assessing a prospective inequality. That was
outside HC-28's explicit no-angle-sweep scope. Its output is discarded and no
claim depends on it. The retained K24C/N36 derivation consists solely of the
single exact rational parameterization, a quadratic identity, rational
interval comparisons and rational segment determinants.

## Verification

- `F(1/6)=2791/50000>0` and
  `F(7/40)=-2297/200000<0` bracket the smaller exact root;
- the closest containment bounds are `959/960<1` and `31/1440>0`;
- substituting the displayed root makes the exact closure polynomial zero;
- the second quadratic root exceeds `2`;
- the two crossing parameters are in `(0,1)`, and the determinant signs are
  `+,-,-,+` with exact values `5/12,-7/12,-7/12,5/12`;
- governed artifact roots remain byte-identical to the HC-28 baseline.
- repository tests: `224 passed, 17 deselected` in 145.03 seconds.

## Decision

HC-28 is exhausted at 3 of 3 sessions. K24W—the existence of a simple point
of the complete non-right family—is open and frozen. No further angle,
orientation, coordinate, solver or topology work is authorized without a new
human checkpoint. No tile or candidate is claimed.

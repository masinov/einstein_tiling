# Session 135 — HC-28 non-right rhombic-family admission

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-28`, session 1 of 3

## Authorization and question

After K21Q reached its fixed resource stop without a verdict, the user
explicitly authorized the previously selected symbolic non-right guard-angle
branch. This is a theorem session, not an experiment, so the experiment gate
does not apply and no research command will be launched.

The exact question is whether changing the right guard angle that created
N33's square-lens squeeze can preserve K9A's binary selector while admitting
the original equal-spoke K10B carrier. The family is fixed in advance:

- `0 < gamma < pi` and `gamma != pi/2`;
- `theta = rho = (pi-gamma)/2`;
- the unchanged 15-edge centrally paired K10B word;
- equal guard legs `d,d` and the induced rhombic half-turn lens;
- positive exact weights with `h=a+b+c`;
- exact open-lens containment, host closure and full spine simplicity.

This changes a named N33 hypothesis rather than changing weights inside its
closed square family or modifying the frozen K16B topology.

## Prior-art boundary

The controlling geometric compiler and one-support prior-art audit is already
recorded in theory note 20 and D-0108. HC-28 claims no novelty for polygonal
linkages, half-angle parameterization, semialgebraic feasibility or symbolic
recoding. Its research target is the repository-specific exact carrier
obligation left by K9A/K9T. No audited source supplies either a witness or a
no-go for this fixed non-right family.

## Terminal outcomes and stop

Within at most three sessions, record exactly one of:

1. an exact simple non-right carrier spine satisfying every fixed family
   predicate;
2. a proof-draft incompatibility theorem for the entire fixed family; or
3. a freeze at one precisely named surviving obligation.

The complete family formulation alone is not success. There is no numerical
angle sweep, solver/QE run, topology change, extra participant, SVG or
candidate promotion. If neither theorem nor exact witness is obtained by
session 137, outcome 3 fires.

## Artifact budget

The checkpoint baseline is
`docs/notebook/assets=6756513353`, `data=1809060890` bytes. This theorem-only
checkpoint is expected to add zero governed artifact bytes.

## Work in progress

K22R derives the complete relative spine using the fixed turn
`q=exp(i*(pi+gamma)/2)` and central half-turn pairing. K22S expresses open
rhombus containment by two exact oblique-coordinate forms at each of six
first-half vertices, adds exact host closure, and identifies the 66
nonadjacent segment predicates needed for simplicity. The half-angle variables
make the whole family polynomial.

At `gamma=pi/2`, the six relative partial sums, containment inequalities and
closure equation reduce term for term to K15S. This verifies that HC-28 changes
exactly N33's right-angle hypothesis. No witness or incompatibility theorem is
claimed yet.

## Verification

- the guard endpoints have equal length `d` and included angle `gamma`;
- the lens basis determinant is `d^2*sin(gamma)>0`;
- central inversion maps oblique coordinates `(alpha,beta)` to
  `(1-alpha,1-beta)`;
- `13` spine segments give `78-12=66` nonadjacent pairs;
- substituting `q=(-1+i)/sqrt(2)` reproduces all six K15S partial sums.

## Decision

Session 135 closes only the mandatory family derivation. HC-28 remains active
at session 1 of 3; family formulation alone is not success.

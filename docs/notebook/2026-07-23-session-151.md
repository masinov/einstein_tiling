# Session 151 — HC-34 six-cell exact-decision pre-registration

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-34`, session 1 of 3

## Experiment pre-registration

### Proposition

Decide the six fixed bounded K16W cells left by corrected HC-33:

```text
S1--, S1-+, S2--, S2-+, S3--, S3-+.
```

Every cell retains complete normalized K21Q, the surviving `P_+-` polarity,
N38/K29O, N41/K31C's exact bounds, corrected N42's H-east sign, one complete
K32S strand sign system, K32A's bounded bridge charts with K33C's first sign
negative, host closure, all 32 containment bounds and all 120 exact closed-
segment nonintersection predicates.  The list exhausts K16W by N41, N43 and
K33C; no S4 or positive-first chart is reintroduced.

### Prior art and non-redundancy

Jovanovic--de Moura NLSAT and Z3's QF_NRA implementation were audited for
K21Q in sessions 134 and 143; no solver or quantifier-elimination novelty is
claimed.  HC-31's two unbounded polarity formulas both reached resource
stops.  N41 subsequently refuted one polarity, K31C bounded every weight,
ERR-013 corrected the strand traversal, and K33M/N43/K33C reduced the live
domain to six bounded cells not previously serialized or run.  The research
object is the fixed carrier obligation and its exact certificate boundary.

### Outcome decisions

- A SAT result reopens only that carrier cell, and only if the independent
  exact verifier confirms the original geometry, its chart/strand signs and
  all 120 pair predicates.  It is not a polygonal monotile, placement patch or
  aperiodicity result.
- Bare solver UNSAT is recorded as solver evidence but does not close a cell
  without independently replayable exact evidence.  All six cells close K16W
  only if every one receives such a certificate.
- `unknown`, external resource stop, killed process, malformed result or an
  unverifiable/algebraic model leaves that named cell open/frozen.  It does
  not authorize a rerun, alternate ordering, extra cell or larger budget.

### Stop rule and finite justification

Run exactly two printed concurrent batches:

```text
batch 1: S1--, S1-+, S2--
batch 2: S2-+, S3--, S3-+.
```

Each cell receives one Z3 5.0 QF_NRA ordering, an internal three-hour timeout,
an external `/usr/bin/timeout` at 10,800 seconds with 60-second kill grace,
and a 32-GiB process memory parameter.  Maximum solver work is eighteen
cell-hours; maximum elapsed solver time is six hours.  Preserve every SMT-LIB
formula, SHA-256, log/result and one aggregate manifest.  No fallback formula,
seed, ordering, partial cell, weakened simplicity predicate, sequential retry
or post hoc budget increase is allowed.

### Human checkpoint

This is session `+1` under `HC-2026-07-23-34`.  Baseline governed roots are
`docs/notebook/assets=6757298033` and `data=1809060890`; six formulas and
manifests are expected to remain far below the 1-GiB checkpoint limit.  The
user explicitly authorized HC-34 after reviewing the corrected six-cell
HC-33 result.

## Results

The experiment gate passes.  The exact builder now accepts only the six fixed
HC-34 cell IDs.  It substitutes K32A's bounded charts, adds K31C and corrected
N42, and clears the three K32S strand sign systems without new geometric
variables.  Every cell has 187 top-level constraints: 13 base, 32 scalar
containment, one closure, all 120 nonadjacent-pair predicates, and 21 admitted
decomposition/chart constraints.

The cold rational verifier independently reconstructs each selected bridge
chart, polarity, bounds, H-east sign and strand-order numerators before
checking the original geometry and all 120 pairs.  A fixed formula serializer,
single-cell runner and two-batch external supervisor were written only after
the gate passed.

## Verification

Targeted construction/governance tests pass (`12 passed`), all new Python
files compile, and the complete suite passes: `230 passed, 17 deselected` in
143.77 seconds.  Governed artifact roots remain byte-identical before formula
serialization.

## Decision

Commit the validated implementation first.  Then serialize all six formulas
through `scripts/run_research.py`, recompute their hashes/counts, and only if
the cold manifest is complete launch the preregistered screen job.

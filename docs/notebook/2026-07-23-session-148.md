# Session 148 — HC-33 K16W thin-lens reset theorem

**Date:** 2026-07-23

**Checkpoint:** `HC-2026-07-23-33`, session 1 of 3

## Target

Derive exact inequalities for the sixteen K32S/K32A cells and attempt
theorem-level elimination before constructing any solver formula.  Every
elimination must be a necessary implication from complete K16W or an exact
equivalence; no pair predicate, boundary stratum or chart may be dropped.

## Results

1. A general same-polarity reset lemma bounds the length of the path between
   two long directed segments in a width-`v` strip.
2. The C strand is westward and C' eastward.  The central H strand must choose
   one of those two polarities, so either `C--H` or `H--C'` is a same-polarity
   pair separated by exactly the `c,1` path.
3. This forces `M<c+1+delta(v)`, where
   `M=sqrt((a+b+c)^2-1)`.
4. K30W/K31C and exact radical comparisons prove the reverse chain
   `M>10/3>c+1+delta(v)`.
5. **N42** therefore refutes complete K16W.  The same contradiction applies
   to every one of the four strand orders and four bridge-chart pairs, so all
   sixteen HC-32 cells are empty.

## Verification and disposition

The proof is exact and theorem-only.  It retains the complete K16B topology,
containment and simplicity hypotheses; it does not reinterpret either HC-31
resource stop.  No formula, solver, coordinate fit, angle sample, polygon,
SVG, patch, decoder, aperiodicity result or candidate was produced.

The exact arithmetic comparisons are pinned by a regression test.  The full
suite passes: `227 passed, 17 deselected` in 141.47 seconds.  Governed artifact
roots remain byte-identical to the HC-33 baseline:

```text
docs/notebook/assets  6757298033 bytes
data                  1809060890 bytes
```

HC-33 reaches a terminal mathematical outcome in its first session.  The
planned serialization and decision run are not activated because there is no
surviving cell.  K16W closes as a refuted fixed-topology witness obligation;
broader monotile and guard-and-shield questions remain open.
